# 发布前手测清单（约 5–10 分钟）

改完 `iMedicalLIS-enhancer.user.js` 并 bump 版本后，在真实 LIS 上按下列路径点一遍。

## 0. 更新脚本

1. 确认 `python3 ~/脚本/serve.py` 在跑  
2. Tampermonkey → 本脚本 → 检查更新 / 重新安装  
3. 刷新 LIS 页面，控制台或悬浮钮确认版本号正确  

## 1. 一键批审（待审视图 · 正常标本）

> v8.5.0 起「正常」「异常」两个分类已合并为「🔍 待审」单视图：同一列表里危急→异常→正常，
> Enter 逐条审核任意行，F4 批审全部正常，审完不切标签、队列清空即显示空态。

| 步骤 | 期望 |
|------|------|
| 打开工作台 | 默认进入「🔍 待审」视图，分类栏只剩 待审/不完整/待排/全部 |
| 待审视图顶部横幅 | 显示 ✅正常 N · ⚠️异常 M ·（🚨危急 K），正常>0 时有「⚡ 一键批审正常 N · F4」按钮 |
| 列表排序 | 同一仪器内：危急 → 异常 → 正常；危急行红边置顶并有 🚨 徽章 |
| **异常结果 chip（8.11.12/15）** | 长中文名显示为规范缩写（MPV/NEU#/ALT/ApoA1/Cys-C/sdLDL-C…，血常规+生化主流项目全收录）+ 数值 + 高↑/低↓ 箭头；悬停 chip 弹全称与方向；卡片/chip 高度收紧，一屏可容纳更多标本 |
| **血常规次要异常折叠（8.11.13）** | 比率/分布宽度/MPV 等衍生项不单独出 chip，折叠为「+N 次要」虚线 chip；悬停可见被折叠项全量；危急值/core（WBC/RBC/HGB/PLT/五分类计数）永不被折叠；cond 项（HCT/MCV/MCH/MCHC/网织红#）超阈值照常显示；非血常规组合完全不受影响 |
| **未收录项目名** | 超长名截 8 字显示，悬停仍可见全称 |
| **待审视图按 F4（8.11.11 免确认）** | **直接开始批审**正常标本，Toast「⚡ 开始批审 N 个…」+ 进度条出现；不再弹确认框 |
| **数字键 1-5 切分类（8.11.14）** | `1`待审 `2`不完整 `3`待排 `4`采集 `5`全部 直切；搜索框等输入框内打数字不受影响；详情面板/历史浮层打开时不响应 |
| **Alt 系快捷键已移除（8.11.14）** | 原生页 Alt+A/B/N/P/R/D 全部失效（含 Alt+B 旧确认弹窗链路一并删除）；批审唯一入口 = F4 / 横幅按钮 |
| **横幅「⚡ 一键批审正常 N」按钮点击** | 同 F4，直接批审（危急/异常/不可自动审核标本本就被排除） |
| 批审进行中再按 F4 | 提示「正在审核中，请稍候」，不会叠加启动 |
| 分类进行中按 F4 | 提示「标本正在分类中…」，不启动（分类完成后可再按） |
| 打开某条详情时 F4 | **无任何反应**（8.5.1 起详情内 F4 已取消：不审详情、也不弹批审框） |
| 一键批审启动后 | 进度条出现，文案随阶段变：选中 / 详情 / 审核中 Ns / CA… |
| 首条若需 CA | 进度显示「CA 认证中（capping）」**只认证一次**；成功后关窗，同组秒审。**不应**反复「CA 重试」toast，**不应**卡在二维码/capping 反复切换 |
| 批审过程中点「停止」 | 当前条结束后停，不整页卡死；自动模式停止后不再启动下一条 |
| 自动审核网络瞬断/工作台保留旧数据 | 自动审核暂停，不使用旧列表继续审核；恢复全量刷新后才继续 |
| 自动审核成功确认 | 自动模式只接受目标状态变为 `3`；行消失、弹窗或软提示不能单独记为成功 |
| 传染病项目 | 梅毒/丙肝/艾滋阳性跳过留人工；乙肝两对半 5 项阳性可按正常分类结果自动审核 |
| 负值结果红线 | 含负值结果（如 -1.3）的标本无论正常/异常分类都不进自动审核，留人工（记录日志「含负值结果」） |
| 偶发某条较慢 | **最多约 8s（已 CA）或约 48s（需 CA）** 后跳过/重试，不整批挂起 |
| **已 CA 后批审** | 进度应显示「秒审」；**每条通常 <2s**，不应每条读秒数秒；**不应**每条弹「审核登录窗口」Toast |
| 结束后 | Toast 显示成功/失败/跳过；列表刷新；已审行从列表消失 |

**回归关注点（曾出现）：**
1. 进度半天不动、最后又提示成功 → 应有秒数心跳，且不超过单项硬超时
2. 已 CA 后批审仍每条很慢 + 右上角审核登录提示 → 8.4.x 已修（Ukey 用户对齐 + 窗口误检）
3. **8.5.35 速度回归**：批审每条不再强制刷新原生工作列表（同机台标本省 1 趟服务端查询，跨机台照常刷新）；
   延迟校验改为 250ms 轮询早退。手测：**同机台连续 5 条以上批审**，观察每条「秒审」节奏是否明显加快；
   **跨机台切换**（生化→免疫组标本相邻）仍应自动刷新列表并正常选中，不出现「原生列表未找到」异常增多

| **待审卡片四色分层（8.14.0）** | 卡片左侧色条+淡底：🚨**红**=危急 / **橙**=超带异常需人工 / **蓝**=轻微可批(F4) / **绿**=正常；轻微可批卡徽章 ⚡、超带 ⚠️；同色标本在仪器段内聚拢（组内排序：危急→超带→轻微→正常）；横幅计数同配色 |
| **四色与批审口径一致性（8.14.0）** | 蓝色数量 = 横幅「⚡ 轻微 N」= F4 轻微异常入队数；蓝色+绿色 = F4 能审的全部；红色/橙色 F4 永远不碰 |

## 1.5 工作台分类栏（8.5.31 新增「采集」）

> v8.5.31 起工作台新增「🩸 采集」分类 = 病房已采但未送到科室的标本。
> 数据来源：`QryStatVisitStatusDetail` P1=9（与 LIS 首页「标本状态汇总」resArr[9] 同源），
> 与现有 待排样（P1=0）完全独立、可同时存在。

| 步骤 | 期望 |
|------|------|
| 打开工作台 | 分类栏 5 个 tab：🔍待审 / 📋不完整 / 📝待排 / 🩸采集 / 📃全部 |
| 🩸采集 tab 计数 | 与 LIS 首页「标本状态汇总 - 采集」列当前工作组数一致（DXI800 若为 6，此处也是 6） |
| 点击 🩸采集 tab | 列表显示 🩸 横条「以下标本为病房采集中、尚未送到科室 · 仅供追踪/催送」 |
| 采集视图行字段 (8.15.26) | 仪器 / 姓名 / 性别/年龄 / 检验号 / 登记号 / 医嘱 / 标本 / 申请科室/床号 / 申请医生 / 采集时间 / 忽略 |
| 点击 📝待排 tab (8.15.26) | 列表显示 📝 独立只读视图与横条「以下标本已送达/核收 · 待上机排样测试」 |
| 待排视图行字段 (8.15.26) | 仪器 / 姓名 / 性别/年龄 / 检验号 / 登记号 / 医嘱 / 标本 / 申请科室/床号 / 申请医生 / 核收时间 / 忽略 |
| 点击某行 | 打开详情面板（采集/待排标本仅供追踪核对，无审核权限） |
| F4 / Enter | **不应触发批审或逐条审核**（`_auditFromDetailPanel` 对采集/待排加守卫，弹「未送检/未核收，不可审核」） |
| 切回「📃全部」 | 采集标本也在列表里，能用状态列区分 |
| 菜单栏下拉 | 多一行 「采集中 N」（图标 `:drop.fill:`），数字与 LIS 首页一致 |
| 该仪器送检后 | 工作台下次自动刷新（30s 轮询），标本从「采集」滑入「待排/不完整/待审」 |

### 1.5.1 筛选行分组可读性（8.16.6）

> 仪器多时，仪器小标签会一直铺到右边界，与右侧「待审/不完整/待排/采集/全部」贴在一起
> （两组之间只隔 6px、且状态标签本身无容器），看着像同一排标签，分不清谁是谁。

| 步骤 | 期望 |
|------|------|
| **仪器多的工作组**（如「全部仪器」下 6 个以上仪器） | 「仪器小标签」与「待审/不完整…」之间有**一条竖分隔线 + 10px 间距**，两组不再贴脸 |
| 状态标签外观 | 5 个状态标签套在**浅灰描边圆角容器**里（与左侧「临检/生化/免疫/全部」分段控件同款），一眼看出是「状态切换」而不是又一个仪器筛选 |
| 仪器标签溢出 | 被右边界截断的那个仪器标签应**渐隐淡出**（不是硬切断），提示还有仪器没显示完；横向滚动/滚轮可继续看 |
| 仪器少时（如只有 2 个） | 渐隐不可见（内容到不了右边界），不应出现「最后一个标签被白白淡化」 |
| **下拉浮层必须正常（8.16.6 关键）** | 点工作组下拉（如「免疫 ⌄」）→ 仪器勾选浮层**正常弹出且完全可见**。⚠️ 8.16.6 的渐隐**不能用 `mask-image`**：实测 mask 会把 `position:fixed` 的后代整块裁掉（坐标对但不可见），浮层会「点了没反应」；现用 `::after` 覆盖层实现，不影响任何后代 |
| 窄屏（<700px） | 分类栏仍能换行（`.ws-cat-hd-inline{flex-wrap:wrap}`），容器不撑破布局 |

**回归关注点：**
1. 加载速度不应变慢（8.5.32 起采集与待排 API 真正 Promise.all 并行；8.5.31 实为串行）
2. 若某仪器「采集」计数 0（最常见情况），tab 上仍显示 0、不消失
3. 切到「采集」后刷新页面 → 仍停留在采集分类（8.5.32 修 WS_CATEGORIES 漏加 collected）
4. 全部视图里「采集」状态徽章有紫红配色（8.5.32 补 .st-9t）
5. **复检/复审标本（8.5.50）**：状态 4（复审）标本——结果不完整 → 「📋不完整」；结果完整 → 「🔍待审」显示；状态 3（审核）仍归已审核隐藏，不误伤正常已审核标本
6. **复检标本可审核（8.5.52）**：非危急值的复审标本在待审可正常 Enter/批审审核（8.5.50 曾全拦，8.5.52 修正）；危急值复审标本仍被红线拦截
7. **复检标本审核确认（8.5.53）**：复检标本（审核前 status 4）审核成功判定只认**状态变 3 或动态信号**（IsSaveSuccess/IsAuthed），不再被「审核前就是 4」误判成功、不再提前跳过 CA 认证——需确认审核时正常弹 CA/capping 密码认证并审成
8. **复审小标识（8.5.51）**：复审标本在**待审卡片 / 不完整表格**的姓名旁都有紫色「复审」徽章（全部视图已有状态徽章）
9. **审核+复审双标识（8.5.54）**：复检标本审核成功后（状态 4→3），「全部」视图显示**「审核」+「复审」**两个徽章（脚本审核成功时本地记录"曾复审"，7 天有效）；原生 LIS 审核的复检标本无记录则只显示「审核」
10. **危急值红线（保持）**：危急值标本任何入口都不能在工作台审核，必须回原始 LIS

### 1.5.2 仪器小标签简写（8.16.7）

> 仪器名太长（「DXI800化学发光仪」…），一排标签铺满整行。现在**只在选中某个工作组时**的
> 那排仪器小标签用短名，规则：组内**类别名不冲突**就用类别名（血细胞/血凝/尿常规/生化…）；
> 同一组里**多台同类**（如免疫组 4 台发光仪）改用**型号**（DXI800/MAGLUMIX8/Getein1600/Wan200+）；
> 两者都拿不到就**保留全称**（不猜）。

| 步骤 | 期望 |
|------|------|
| 选「免疫」工作组 | 仪器小标签显示 **DXI800 / MAGLUMIX8 / Getein1600 / Wan200+ / 手工杂项**（型号），一排明显变短 |
| 选「临检」工作组 | 各仪器类别不冲突 → 显示 **血细胞 / 血凝 / 尿常规** 这类类别名（不是型号） |
| **悬停任一简写标签** | tooltip 显示**完整仪器名**（如「DXI800化学发光仪」） |
| **「全部」模式的下拉菜单（8.16.7 关键）** | 展开工作组下拉（如「免疫 ▾」）→ 里面**仍是全称**（「DXI800化学发光仪」），不简化 |
| 「全部」模式的组按钮 | 仍显示工作组名 + 台数（「免疫 · 2台 ▾」），不受影响 |
| **工作台其它位置（关键）** | 卡片上的仪器名、详情面板、导出、菜单栏、Bark 推送里**一律仍是全称**——简写只在那一排标签 |
| 两台同名仪器 | 若简写后会撞车（如两台都叫 BC5390）→ **两台都保留全称**，不出现两个一样的短标签 |
| 标签瘦身 | 标签内边距/间距/勾选框都比 8.16.6 略小，一排能多放下两三台仪器；字号未变（保证看清） |

## 1.6 待排/采集忽略（8.5.33 新增，8.5.34 修复勾选/计数细节）

> 待排(0)/采集(9) 视图每行有「🙈忽略」按钮；忽略后该行灰显+删除线、不计入任何统计，
> 但列表仍显示，可随时「👁取消」。忽略列表存 localStorage（LIS_WSIgnore），核收后自动释放。

| 步骤 | 期望 |
|------|------|
| 待排视图某行点「🙈忽略」 | 该行灰显+删除线；分类栏「📝待排」计数 -1；footer「可见/总」都 -1（8.5.34：不再出现可见>总倒挂） |
| 忽略后再点「👁取消」 | 恢复原样，计数 +1 |
| 忽略行 checkbox | **禁用、不可勾选**（8.5.34：忽略=不再管它；全选也不会带上） |
| 已勾选的行再忽略 | 勾选自动取消（8.5.34：忽略后 wsChecked 同步删除，F5 不会带上） |
| 「全部」视图 | 被忽略的待排/采集行灰显但仍在列表；分类栏计数已排除 |
| footer 显示 | 「可见 N / 总 M」始终 N≤M，不出现 5/3 类倒挂（8.5.34） |
| **核收自动释放（必测）** | 忽略一条待排 → 在 LIS 核收该标本 → 工作台下次刷新（30s）→ 该行恢复计数、不再灰显、忽略按钮消失（进入未审核/审核流程）。**若核收后仍未恢复 → 说明待排接口 Labno 与主列表格式不一致，把该标本的 Labno 前后形态发我** |
| 刷新页面/换标签 | 忽略状态保持（localStorage 持久） |
| 采集标本点忽略 | 同样灰显+计数排除（采集视图与全部视图均可操作） |
| **待排标本详情按 Enter（8.5.34）** | 弹「待排标本未核收，不可审核（核收后进入待审）」，不拿假 DR 去原生找标本 |

**回归关注点（8.5.34）：**
1. 忽略行在「待审/不完整」分类本就不出现（状态 0/9 才可忽略），无需处理
2. 取消忽略后 checkbox 恢复可用、可正常勾选

## 2. 待审视图逐条审核（Enter / F4）

| 步骤 | 期望 |
|------|------|
| 焦点在**异常/危急**行 | 底部提示 Enter=审核；按 **Enter** → 当前条审核中（卡片显示正在审核…）；成功 Toast「已审核」、卡片移除、焦点下一条 |
| 焦点在**正常**行 | 按 **Enter** → 直接单条审核（信任分类结果）；成功同样移除并下一条 |
| 连按 **Enter** | 当前条审核中可「排队下一条」；不重复弹窗（F4 在待审列表为批审入口，详情内已取消） |
| 危急值行按 Enter | **强制拦截**：提示「🚨 有危急值，必须在原始LIS中审核」，自动跳下一条 |
| 审完最后一条 | **不切标签**：待审视图显示「📭 暂无标本数据」空态（v8.4 会自动切回正常，v8.5 起不再切） |
| 与批审互斥 | 批审进行中 Enter/F4 提示稍候 |

## 2.5 详情面板连续审核（8.5.1 主推流程）

> 主流程：点击任意样本（正常/异常）→ 打开详情 → **Enter 审核** → 自动跳下一条详情 → 再 Enter…
> 详情面板内 **F4 已取消**（8.5.1），只保留 Enter + 「✅ 审核」按钮。

| 步骤 | 期望 |
|------|------|
| 点击任一待审行 | 打开详情面板；焦点自动回到面板（refocusDetailPanel，不被 iframe 吃掉） |
| 详情内按 **Enter** | 审核当前标本，按钮显示 ⏳ 审核中…；成功 Toast「审核成功」；**自动跳下一条待审标本的详情**，焦点保持在面板可继续 Enter |
| 点「✅ 审核」按钮 | 与 Enter 完全一致（同一函数 _auditFromDetailPanel） |
| 详情内按 **F4** | **无任何反应**（已取消；不再审详情、也不弹批审框） |
| 焦点在原生 iframe 内时 Enter | 由常驻 Enter 桥捕获，仍审当前详情（不丢键） |
| 正常行详情内审核 | 正常标本可在详情中审核（classCtx=normal）；危急值在详情内仍被拦截 |
| **异常审完自动跳到正常样本详情 → 再 Enter** | **必须能审**（8.5.2 修复：分类上下文按当前标本实时桶判定，不再沿用旧 source 误拦「已分类为正常」） |
| **连续审核不卡（8.5.3）** | 审完一条自动跳下一条后，后台已预热下一条（定位原生页+加载详情）；再按 Enter 应**秒审**，按钮不应长时间停在「⏳ 定位标本…」 |
| **详情内 ↑↓ 切换后 Enter（8.5.7）** | 详情切到标本 B 后按 Enter：不卡「⏳ 定位标本…」、不误报「未找到标本/详情未就绪」（预热与审核不再双线程抢原生页） |
| **详情打开时点原生 iframe 再 Enter（8.5.7）** | 第一个 Enter 也能审当前详情，不被吞键（Enter 桥在详情打开/关闭时已同步 active 状态） |
| **焦点在搜索框按 Enter（8.5.7）** | 不触发审核（详情/列表的 Enter 处理器已加输入框守卫）；焦点在详情面板按钮上按 Enter = 点按钮（关闭/审核），不抢为审核 |
| ↑↓ / Esc | ↑↓ 切换详情标本；Esc 关闭详情 |

## 3. 详情面板审核

| 步骤 | 期望 |
|------|------|
| 异常/正常卡点开详情 → 点审核 / Enter | 成功后自动跳下一条详情或关闭 |
| 危急值 | 拦截，提示须在原生 LIS 审核 |
| 详情内 F4 | 已取消（8.5.1），无反应 |

## 4. 质控导出（SheetJS 本地）

| 步骤 | 期望 |
|------|------|
| serve 未开时导出 | 可能提示 XLSX 未加载 |
| serve 开启后刷新再导出 | 能生成 xlsx，**不依赖公网 CDN** |

**数据正确性回归（8.4.6/8.4.7 曾导错，必测）：**

| 检查点 | 期望 |
|------|------|
| 成对项目不串位 | 同一批数据里 **FT3≠TT3、FT4≠TT4、F-PSA≠PSA**，数值与原生质控图一致 |
| 凝血 D-二聚体 | 导出含 D-二聚体行（`D二聚体`/`D-Dimer` 命名也能匹配，不缺失） |
| 血常规双浓度 | Level 1 与 Level 2 **两行数值不同**（不是同一数重复），批号分别 N/H |
| 水平归属 | 各水平数值与「原生质控图对应水平」一致，不整组串到 Level 1 |
| 项目齐全 | 9 组全部项目都有行，无静默缺失 |
| 数据量 | 导出行数 ≈ 各组质控记录数（不翻倍、不缺少同日多次） |

## 5. 登录 / 密码

| 步骤 | 期望 |
|------|------|
| 记住审核密码 / CA 密码 | 下次自动填（HTTP 下为可逆存储，勿在共享电脑使用） |
| 设置里可清除 | 清除后不再自动填 |
| **切换工作组（CA 认证）** | **Chrome 不应弹「保存/更新密码」提示**（8.5.30：所有自动填密码处填值前先标 `autocomplete=new-password`，且 iframe 补扫改为递归嵌套；CA 密码框在嵌套 iframe 里，之前靠扫描时机可能漏标导致偶发弹窗） |

### 5.1 多用户 CA 账号（8.5.36）

> 支持保存多组 CA/capping 账号（用户名+密码），认证时按当前登录用户名自动匹配，匹配不到用「默认账号」；
> 认证失败时弹确认，可选其他已存账号重试。

| 步骤 | 期望 |
|------|------|
| 设置→密码管理 | 「CA 认证账号」区显示账号列表；旧单密码首次会并入「当前登录用户名」账号（不丢失） |
| 新增账号 | 用户名 + 密码（+备注）→ 点「新增/更新」→ 列表出现该账号，标「已存密码」 |
| 设默认 | 点某账号「设默认」→ 该账号标「CA默认」，认证时优先用 |
| 编辑账号（8.5.37） | 点某账号「编辑」→ 表单回填该用户名与备注，密码留空 = 不改；改备注/密码后点「新增/更新」→ 保存并标「已更新」；点 ✕ 退出编辑 |
| 编辑改用户名（8.5.37） | 编辑时改用户名 → 保存后旧账号删除、新用户名生效，默认标记跟随新用户名 |
| 删账号 | 点「删除」→ 账号移除；若删的是默认，默认标记同步清除 |
| 当前登录 | 与当前 LIS 登录用户名相同的账号标「当前登录」 |
| **批审触发 CA** | 用默认/当前登录账号自动 capping 认证；CA 窗用户名 = 该账号的用户名（不再是登录用户名） |
| **认证失败** | 弹确认「是否改用其他已存账号重试？」→ 选一个 → 用该账号立即重认证并记为默认；选取消则停在当前账号 |
| **切换账号重试（8.5.43）** | 当前登录用户自己也有 CA 账号时，认证失败后选其他账号仍能生效（8.5.43 修：原来重查默认账号会劫持回当前登录账号，切换无效） |
| **删除当前登录账号（8.5.43）** | 旧版单密码并入的账号可正常删除，不会被旧 K.caPwd 复活（8.5.43 修：legacy 迁移一次性落盘并清旧 key） |
| 多账号并存 | 用不同用户密码分别认证，Ukey 各归各，不互相覆盖 |

### 5.2 登录弹窗选账号 + CA 跟随当前登录（8.5.38）

| 步骤 | 期望 |
|------|------|
| 打开快速登录弹窗 | 用户名框**预填上一次登录的用户名**；点右侧 **▾** 图标弹出账号选择浮层（显示「用户名 · 备注」），点选即填入用户名 |
| 从下拉/浮层选某账号 | 用户名框填入该用户名，手动输密码后登录 |
| **登录后触发 CA 认证** | 用「当前登录的用户名」的 CA 账号认证（`caDefaultAccount` 优先级：**当前登录用户 → 显式默认 → 首个有密码**）|
| **用另一个账号登录** | 只要该账号在 CA 账号里存过密码，CA 认证即改用该账号，**不再是之前设的默认账号**（8.5.38 修：之前默认账号优先级最高，换登录账号不会生效）|
| **换账号不删预填（8.5.39）** | 用户名框已预填上次用户时，不用清空，直接点 ▾ 选新账号即覆盖 |
| 想让某账号固定认证 | 仍可在设置里「设默认」，但当前登录用户有账号时会优先跟随登录用户 |

### 5.3 工作台 CA 账号徽标 + 空数据保护（8.5.40）

| 步骤 | 期望 |
|------|------|
| 打开工作台 | 分类栏右侧显示当前 CA 账号的**备注名**（无备注则显示用户名；完整账号信息在悬停提示里） |
| 登录/切换账号后 | 徽标随当前登录账号变化（CA 认证成功后立即刷新） |
| **长时间闲置 / 网络瞬断** | 30s 自动刷新若拿到空数据，**保留原有统计不清零**（8.5.40 修：原阶段1空数据会直接清空 wsData → 全 0） |
| **跨天（8.5.42）** | 日期变化后下次自动刷新**清空重载归零**（工作台按当天查询，昨天的标本在原生列表已查不到，保留旧数据只会显示「看得见审不了」的假数据）；同一天内瞬断仍保留 |

## 6. 病人结果导出（外送 + 费用）



| 步骤 | 期望 |
|------|------|
| 打开「结果」面板 | 工作组树含 **外送**，四组同屏可见、**无需横拖**（审核台/质控台仍无外送） |
| 只勾外送仪器 → 查询 | 能列出外送标本结果（即使工作列表 ResultFlag=N 也读明细） |
| 导出 CSV | 含列：报告费用、医嘱费用、项目费用、医嘱费用明细 |
| 报告费用 | 与原生报告页「费用」一致；**按检验号去重**再汇总金额 |
| 参考范围列 | 完整保留，如 `110-160` 不被截成 `10`、`100-200` 不被删空（cleanRefRange 回归） |
| 审核工作台 | 工作组标签仍只有临检/生化/免疫 |

## 7. 快速冒烟（可选）

- 病人结果筛选导出一行
- 质控录入页辅助浮层出现
- 切换工作组后批审队列续跑（若有未完成队列）
- 菜单栏下拉：点「待审」行 → 工作台切到待审视图（不再有 可批审/异常待审 两行）
- 老版本 localStorage（cat 为 normal/abnormal）→ 打开工作台自动归一到待审视图

## 8. 跨组审核（8.5.10：跨组先试不切组，选不到才切组；8.5.8 起支持多组多仪器一次审完）

> 前置：工作台勾选/显示多组仪器（跨组勾选保留），待审视图已按「当前登录组优先」排序。
> **危急值红线：任何入口都不允许在工作台审核，必须回原始 LIS。**
> **自动审核红线（8.5.74）：梅毒、丙肝、艾滋阳性固定留人工；乙肝两对半五项不在自动审核红线内。自动审核设置中不再提供可关闭的传染病选项。**
> **自动审核记录查看器（8.5.75）：完整项目结果随日志持久化（短键 items），样本离开工作台/缓存被清后仍能展开看全部结果；日志总量超预算时最早轮次降级为摘要。**
> **8.5.10 核心变化**：跨组标本不再立即切 LIS 工作组——原生报告页可按 WorkGroupMachineDR 直接
> 跨组加载并审核（实测：临检组下免疫组x8 标本可直接审），故列表/详情/批审都先**不切组直接审**；
> 仅当原生列表选不到标本或详情加载不出（prep.reason='select'/'detail'）时才回退 safeSwitchWG 切组重试。

| 步骤 | 期望 |
|------|------|
| 待审视图排序 | **当前登录工作组的标本排最前**（先审完本组）；其余组按字母序在后 |
| **F4 批审正常标本（跨组）** | 队列按「当前组优先」排，本组先审；遇其他组**先不切组直接审**（原生列表自动切到目标仪器）；选不到标本才切组续跑；**全部审完若从未切组则不切回**，若切过组则自动切回起始工作组并重开工作台 |
| 列表逐条 Enter 审异常/正常（跨组） | 审到别组标本**先不切组直接审**；选不到才自动切组；该组标本连续审完再切下一组（单向推进）；**审完自动切回起始组（仅当切过组）** |
| 详情面板连续审（跨组） | 详情审到别组标本**先不切组直接审**；选不到才切组 → 审完目标条后**自动重开详情面板**显示下一条 → 继续 Enter；**审完最后一条自动切回起始组（仅当切过组）** |
| **危急值标本** | 列表/详情任何操作都**强制拦截**：提示「🚨 有危急值，必须在原始LIS中审核」，自动跳下一条；**绝不自动审、绝不进批审队列** |
| 中途手动切组 | 不破坏跨组流程；origin 30 分钟有效，超时后以当前组为新起点 |

**回归关注点（曾出现）：**
1. 跨组后切回 origin 不出现死循环（K_AUDIT_ORIGIN 审空才消费、30min 过期、K_ABN_TGT cycle 上限 3）
2. 详情跨组恢复后焦点在详情面板（scheduleAbnormalFocusRecovery 已加详情守卫，不抢焦点）
3. 切回后工作台自动打开（switchBackToOriginWG 设置 WS_REOPEN_KEY，重载后 maybeReopenWSAfterReload 恢复）
4. **分类未完成时审空不提前切回**：切回条件要求待审视图真空 + 无搜索过滤 + 分类未进行（trySwitchBackToOrigin 最多等 30s），大工作组分类滞后时不会遗留标本
5. **8.5.10 不切组直接审时**：跨组标本的原生列表仪器列可能显示为裸 DR（如“17”）而非名称——这是预期（当前登录组没有该仪器的名称映射），不影响审核
6. **8.5.10 回退切组的触发条件**：仅当 `ensureSpecimenReadyForAudit` 返回 `reason='select'`（原生列表选不到标本）或 `reason='detail'`（详情加载不出）时才回退 `safeSwitchWG`；若直接审成功则不切组、不切回

## 9. 患者历史浮层（8.5.23 新增，周一用真实病人必测）

> 详情面板头部「🔎 历史」按钮 / 详情内按 **H**。数据源：`QueryReportList&RegNo=` 列该病人历次报告 → `GetReportInfoAll` 逐份取项目 → 按项目聚合为时间线。
> **⚠️ 重要：这是新功能，数据路径尚未在真实病人上验证过。** 手工/无档案病人（RegNo 为空）无法查历史，属预期；
> 请务必用**有历史的真实病人**（例：复查糖化、梅毒随访）实测，异常时点「🔍 调试」把内容发我。

| 步骤 | 期望 |
|------|------|
| 打开一条**有历史**的真实病人详情（糖化/梅毒均可） | 详情面板**顶部已无「历史」按钮**（8.5.28 移除）；整报告历史改为详情内按 **H**；主要入口是结果表每行 🔎 |
| 按 **H** / 点按钮 | 弹出居中浮层，显示病人姓名/检验号 + 历次报告数 |
| 历史加载 | 出现 spinner，随后按组显示：**本次**(当前报告项目，绿底高亮) → **关联**(如糖化→血糖/果糖胺，梅毒→TRUST/TPPA) → 其他 |
| **默认只显示 本次+关联**（8.5.26） | 其他项目默认隐藏；在顶部搜索框输入即可临时全库搜索，清空搜索回到只显示关联 |
| **关联精确匹配**（8.5.26） | 关联改用**家族表**（HIST_FAMILIES，可自行增改）：糖代谢/梅毒/心肌/甲功/肝功/肾功/电解质/血脂/炎症感染/贫血/凝血/乙肝/丙肝/PSA。**糖化不再误关联红细胞项目**（triggerExcludes 排除）；搜索框可直接搜到非关联项目 |
| **炎症/感染家族扩充**（8.5.28） | 降钙素原/CRP/超敏CRP/白细胞计数/中性粒/淋巴/单核/嗜酸/嗜碱/血沉ESR/SAA/IL-6/TNF 互相关联；匹配已**归一化**（去空格/连字符/括号），C-反应蛋白、白细胞计数(WBC) 等都能对上 |
| 关联家族表可改 | `iMedicalLIS-enhancer.user.js` 模块 DH 顶部 `HIST_FAMILIES`：members=成员/触发词、triggerExcludes=触发排除（如糖化）、excludes=关联排除（如尿血红蛋白）。改完 bump 版本重载即可 |
| **单项目历史（8.5.27）** | 详情面板结果表每行 🔎 / 病人结果导出(PR)列表每行 🔎 → 打开**只显示该项目**的历史浮层（跨组时间线 + 其关联家族，如梅毒抗体→TRUST/TPPA），不含报告其他项目；头部「焦点」徽章可 ✕ 退出回全部 |
| 跨组历史 | **必须能看到别的工作组（生化/免疫/临检）的历史结果**（8.5.25：QueryReportList 改为**按各工作组分别查询再合并**，`&WorkGroupDR=<组>` 是原生跨组过滤参数，已实测原生页面多选工作组用它；每份报告带 `_histWG`，取明细直接用它所在组的 ss） |
| 跨组历史·调试判断 | 若仍只有本组：点「🔍 调试」看 `perWg`（各工作组分别返回条数）。免疫14/生化0/临检0 且该病人确实只有免疫历史 → 正常；若病人**明确做过生化/临检**却为 0 → 说明 WorkGroupDR 跨组查询没生效，把 `perWg` 发我，我切到工作列表(prLoadRows)兜底方案 |
| 历史多时可滚动 | **鼠标滚轮可下滑看全部历史**（8.5.24：面板/body 已强制 overflow+flex，与工作台 body 同款处理；若仍不能滚，点「🔍 调试」告知） |
| 每个项目 | 显示历次日期/结果/异常标(↑↓⚠)/来源(医嘱·检验号·工作组)，按日期倒序；「本次」行高亮并有徽章 |
| 顶部搜索框 | 输入 血糖 / GLU / TRUST 等能筛出对应项目组 |
| 关联 chips | 点「葡萄糖」→ 只显示该组；点「全部」复位 |
| 「仅看本次/关联」勾选 | 只显示 tier0+tier1，隐藏无关项目 |
| 日期范围切换 | 近30/90/180天/1年/全部，切换后重新聚合（有缓存，秒级） |
| **Esc / ✕ / 关闭** | 关闭浮层，焦点回到详情，可继续 Enter 审核（历史打开时 Enter 不误审） |
| **详情内 F4** | 仍无反应（历史浮层不影响）；**历史浮层打开时工作台/F4桥全部让位**（8.5.29：防隔层误审，历史开着时 Enter/F4/j/k 都不会审到背后的标本） |
| **🔍 调试** | 显示 QueryReportList 结果、每份报告取数成功/失败+条目数，用于排障 |

**周一验证要点（决定数据路径是否成立）：**
1. 有历史病人的 `QueryReportList` 能返回历次报告（含 ReportDR/检验号/项目/工作组/日期）
2. `GetReportInfoAll` 对**历史（非当前）报告**、**跨组**报告也能返回项目明细（脚本会按当前组 ss → 报告组 ss 依次重试）
3. 糖化报告能看到历史血糖、梅毒报告能看到 TRUST——关联匹配是否如预期
4. 若某份历史报告取数失败：点「🔍 调试」，把 `perReport` 里 `ok:false` 那行的 rdr + err 发我

## 10. 自动审核（8.5.58 新增；8.13.0 起分白天/夜间两方案）

> 头部最右侧「🤖 自动审核」按钮（刷新/CA密码旁，8.5.61 起）：按设定时长自动审核**当前筛选范围**（工作组+勾选仪器，可跨组）的标本。
> 正常走批量引擎、异常逐条过安全门；**危急值 / 堵孔0值 / 传染病阳性** 一律跳过留人工并记入日志。
> **8.13.0 两方案（手工切换，弹窗里点选即生效）**：☀️白天=异常逐条仅限「整管在轻微带内（乙类≤8）」，超带异常留人工（同 F4 批审口径）；🌙夜间=异常逐条全放（红线除外，=8.5.58 起旧行为）。**8.15.5 起缺省为「白天」（保守侧）**——此前缺省夜间，状态被重置会静默落回最宽松口径。按钮状态与剩余时长文案带当前方案。
> 推送三档（8.13.0）：推送所有 / 只推送未成功 / 不推送（原「推送不成功+有异常标本」档已删，旧设置自动迁移为「推送所有」）。
> 工作台关闭时保持运行（自动重开）；状态存 localStorage，页面刷新后自动恢复剩余时长。
> 头部「🕘」/记录查看器（8.5.61 起）：按日期范围浏览/按姓名检验号搜索最近自动审核了哪些样本，**每行直接展示项目组合与异常项目**（8.5.70）。

| 步骤 | 期望 |
|------|------|
| 头部最右侧 | 「🤖 自动审核」按钮在刷新↻/CA密码钥 旁边；分类栏不再显示该按钮（8.5.61 移动）。**开启中按钮变绿色高亮**（8.5.63：深绿底+白字+绿边，hover 加深），停止后恢复普通样式 |
| 点按钮 → 弹窗 | 顶部显示**开启时固定的审核范围**（8.5.67 快照语义，标注「开启时固定」：工作组 + 勾选仪器；组内无勾选=「全选」；全部工作组无勾选=「全部+全选」；仅部分组勾选=只列勾选组+仪器）。**弹窗只做设置，不再内嵌记录**（8.5.70：记录统一由头部🕘查看） |
| 点按钮 → 开启（默认 60 分钟） | 按钮变「🤖 自动审核中 mm:ss」并**每 5s 走秒**；footer 追加「🤖自动审核 mm:ss」；toast「已开启」 |
| 开启后立即 | 无需等 30s：数据就绪（2s 防抖）即跑第一轮 |
| 正常标本 | 自动批量审核：进度条标题「🤖 自动审核中...」，**无确认框、无逐条成功 toast 轰炸**；审完小结 toast「正常 N · 异常 M · 跳过 K」 |
| 空转轮次 | 本轮无任何审核动作（正常/异常/跳过全 0）**不弹小结 toast**（8.5.65：避免开启后几秒弹无意义弹窗）；仅实际审到标本才弹小结 |
| 全部视图 🤖 徽章 | 8.5.66：切到「全部」分类，今日已由自动审核审核的标本（状态=审核）在状态列显示深绿「🤖 自动」徽章；**点击该行 → 打开结果详情**；审完的标本 30s 数据刷新后出现；跨天自动清空 |
| 异常（非危急）标本 | 安全门通过后逐条自动审（卡片高亮「正在审核…」）；成功 Toast 被静默、卡片移除 |
| **危急值标本** | 自动审核**跳过**，出现在「今日记录」原因「危急值，必须在原始LIS中审核」；待审卡片 🚨 不动 |
| **堵孔0值标本** | 跳过，原因「含 0 值结果（疑似堵孔）」；待审卡片显示「0?」徽章 + 「0值? 疑似堵孔，需人工确认」提示 |
| **传染病阳性** | 跳过（原因含「传染病项目阳性」）；**关掉安全规则开关后**才会自动审（不推荐） |
| 手动关闭 | 点按钮 → 停止 → 立即停（toast「已关闭」）；按钮回「🤖 自动审核」 |
| 时长到期 | 自动停（toast「已到期」），状态清空 |
| **关闭工作台** | 自动审核**保持运行**：30s 内自动重开工作台继续审，剩余时长不变 |
| **页面刷新/重开工作台** | 自动恢复（toast「已恢复，剩余约 X 分钟」）；头部按钮显示剩余时长 |
| **勾选部分仪器（开启时）** | 只审开启那一刻勾选范围内标本（快照，跨组正常）；范围外不审；忽略标本不审 |
| **单工作组切组** | 8.5.68：点击工作组标签（如临检）后**只显示并可选该组仪器**；其他组（如生化）已勾选的仪器**自动取消勾选**；跨组勾选只能在「全部工作组」下进行 |
| **开启后勾选/切换/全选** | 8.5.67：不影响自动审核（范围已固定）；30s 内不会审新勾选的仪器。8.5.73：不再弹「范围已固定」提示 |
| **手动 F4/Enter 优先** | 手动审核进行中自动审核本轮让位；自动审核中手动操作提示「正在审核中」 |
| **自动刷新不闪仪器标签** | 8.5.69：30s 自动刷新（部分→全量加载）期间，工作组下的仪器标签**不消失再恢复**（DOM 原地更新，仅出现新仪器才重建加标签） |
| 头部🕘 记录查看器 | 打开记录查看器（弹窗加大 800px/92vh）：样本**直接平铺**，每行 🕘时间 + 姓名/检验号 + 徽章 + 项目组合 + 异常项（8.5.81 起异常项标签也按状态细分配色：高▲橙/低▼蓝/异常⚠粉红/危急🚨深红；8.5.81 之前产生的旧日志无状态信息仍为统一红色）。**点击样本行 → 弹窗内联展开完整结果表格**（项目/结果/单位/参考范围；8.5.80 起按状态细分配色：高=橙底、低=蓝底、异常=粉红底、危急=深红底、待定=灰、0值=黄，徽章文字区分 高/低/异常/危急），**不关闭弹窗、不切到「全部」分类**；缓存清除时回退原始详情缓存或提示 |
| 记录搜索 | 输入姓名/检验号 → 只显示匹配样本（附审核时间），大小写不敏感；无匹配有提示 |
| 记录来源 | 开启自动审核跑几轮后：正常批量与异常逐条的成功样本都进明细（每轮最多 50 条，含项目组合+异常项，8.5.70 起）；跳过样本进跳过名单 |

**回归关注点：**
1. 自动审核期间手动 Enter/F4 不被吞（手动优先）
2. 分类进行中自动审核让位，不打断分类
3. 跨组自动审核：首条 CA 一次、同组秒审；全部审完切回起始组
4. 同标本连续轮询不刷屏：同一跳过原因只记一次（去重）

## 11. 堵孔 0 值拦截（8.5.58 新增）

> 生化仪堵孔会传一堆 0（不能自动审核）；但尿蛋白类参考范围含 0 的合法 0 不能误拦。
> 规则：Z2 同标本 ≥2 个数值 0 = 堵孔特征（核心）；Z1 单个 0 且参考范围不含 0；Z3 单个 0 且与历史不符；Z4 白名单豁免。
> 发光仪堵孔**不传结果** → 空结果 UNCERTAIN，仍不自动审（回归）。

| 步骤 | 期望 |
|------|------|
| 标本含 ≥2 个数值为 0 的项目（如 LDL=0、ALT=0） | 分类为 ZERO：待审显示「0?」徽章 + 黄色左边框；自动审核跳过（原因「含 0 值结果（疑似堵孔）」）；**手动 Enter 仍可审核**（人确认） |
| 单个 0 且参考范围不含 0（如 钾=0，范围 3.5-5.5） | 同样 ZERO 拦截 |
| 单个 0 且范围含 0、无历史（如 尿蛋白=0，首次检测） | **放行**（合法 0，不拦截） |
| 单个 0 且范围含 0、历史非 0（如 LDL 历史 2.5 现 0） | ZERO 拦截（与历史不符） |
| 检出限结果（如 <0.1） | **不算 0**，不触发拦截 |
| 定性项目无数值范围结果为 0 | 不参与 0 值统计（不误拦） |
| **发光回归**：堵孔不传结果 | 空结果 → 不完整/待定 → 不自动审 |

---

## 12. 工作台日期选择 + 跨天自动审核（8.7.0 新增；8.8.0 改为日期胶囊+自绘日历）

> 头部搜索框右侧为**日期胶囊**：`◀` 前一天 / 中间「今天▾」点击**直接弹出日历** / `▶` 后一天（到今天禁用）。
> 日历为**自绘弹层**（不依赖 showPicker，Safari 也能弹）：年月下拉 + 日网格（今天描边、当前选中填色）+ 「今天」按钮 + 「◀前一天 / 后一天▶」底部快捷。
> 查看历史日期时胶囊琥珀色高亮；自动审核运行中查询窗口自动扩展为「开启日~今天」，跨午夜后昨晚未审完的标本仍可见可审。

| 步骤 | 期望 |
|------|------|
| 头部控件 | 搜索框右侧出现 ◀「今天▾」▶ 单个胶囊；查看今天时 ▶ 禁用（半透明） |
| 点 ◀ | 切到昨天：胶囊变「昨天」琥珀色高亮；toast 显示条数；表格/分类计数按昨日数据 |
| 再点 ◀ / 点 ▶ | 前后翻日正确；未来方向到「今天」后 ▶ 禁用，无法进入未来日期 |
| 点中间「今天▾」 | **直接弹出自绘日历**，初始定位在当前查看日期所在月份；再点一次中间按钮可收起 |
| 日历选日期 | 点任意日期 → 日历收起、加载该日标本，胶囊显示 MM-DD 琥珀高亮 |
| 日历「今天」按钮 | 点「今天」→ 日历收起、回到今天视图 |
| 日历 ◀前一天/后一天▶ | 点后切换日期**且日历保持打开**，选中态与月份跟随（可连续步进多天） |
| 日历年月下拉 | 切换年/月后网格正确刷新；今天有描边、当前选中日期有填充色 |
| 日历关闭 | 点日历外任意处 / 按 Esc → 收起 |
| 历史视图数据完整性 | 待审/不完整/待排/采集/全部分类计数与详情面板正常；搜索可用 |
| **历史视图批审** | 选一个有未审正常标本的过去日期 → F4 批审：能选中行、CA 正常、逐条审完；审完后原生页面日期框恢复为今天 |
| **连续 Enter 审核**（8.8.1 回归） | 待审列表点开第一条详情 → Enter 审核成功 → 自动跳第二条 → **立即再按 Enter 能直接审掉第二条**（无需先点列表行）；连续多条全程 Enter 不中断。修复点：预热驱动原生页后焦点被 iframe 可编辑控件抢走导致 Enter 失效，现预热完自动把焦点拉回详情面板 |
| 历史视图强制刷新 ↻ | 该日期无标本时提示「该日期无标本数据」，**不触发整页刷新**（回归：今天视图空数据仍走整页刷新兜底） |
| 刷新页面 | 停在历史视图时刷新页面 → 重开工作台仍是该历史日期；点胶囊弹日历选「今天」或 ◀/▶ 回今天 |
| **跨午夜自动审核**（核心） | 晚上 23:50 开启自动审核（时长覆盖午夜）：0 点后昨晚未审完的标本仍在工作台、继续被自动审掉并进记录；新一天标本照常纳入 |
| 跨午夜后原生列表 | 自动审核昨晚标本时能选中行（原生列表按登记日切过）；队列结束后原生日期框恢复今天 |
| 记录跳转 | 审核记录里点标本的「⤴」→ 自动切换工作台到该标本登记日期并等待数据就绪后弹出详情（8.8.9；跨历史日期无需手动切换） |
| 头部不拥挤 | 日期胶囊占一行内空间明显小于旧版（◀按钮+日期按钮+▶按钮+今按钮四个控件）；标题/搜索框（8.8.3 缩短至 220px）/工作组标签/分类标签/右侧按钮在常用窗口宽度下不换行、不挤压 |
| 8.8.3「今」按钮 | 查看今天时头部无「今」按钮（无需回今天）；切到历史日期（◀/日历选日期）后胶囊内出现「今」按钮，点击一键回今天并隐藏；与日历弹层互斥（点「今」先收起弹层再切今天） |
| 8.8.4「今」按钮位置 | 「今」按钮并入日期胶囊内最后一个区段（◀今天▶今），与日期控件共享边框、永远贴紧，不再独立隔开 |

**回归关注点：**
1. 今天视图（默认）行为与旧版完全一致：单日查询、空数据整页刷新兜底
2. 自动审核范围快照语义不变：只回溯开启日之后的日期，最远 D-7
3. §1 批审 + §2 F4 必测无回归

**8.8.2 新增回归点（跨日期自动审核专项）：**
| 步骤 | 期望 |
|------|------|
| 前天标本同机器跨午夜 | 自动审核开启日=前天（含 23:50 完成的标本），跨过午夜后该标本仍在工作台、仍能被自动审掉（同机器不再被日期挡住——prewarm/ensure 增加登记日变化触发列表刷新） |
| 用户选 >7 天前日期 + 自动审核运行 | 按用户选的日期如实查询，**不再被 D-7 钳制放大**（修复：D-7 只约束自动回溯，不覆盖用户主动选择） |
| 手动单条审历史标本后 | 原生页面日期框恢复今天（auditAbnormalSpecimen finally 收口；自动审核一轮由 autoAuditTick finally 收口） |
| 详情面板审历史标本后关闭 | 原生日期框恢复今天（closeDetailPanel 收口） |
| 关闭工作台 | 原生日期框兜底恢复今天（closeWS 收口） |
| 批审起始对齐 | 跨午夜续审时批审起始刷新直接用首条登记日，首条不再在错误日期的空列表上白等超时 |
| 历史视图查看 + 原生页 | 用户主动在历史日期视图审核时，原生页停在查看日（不强行拉回今天，与查看日一致） |

**8.8.10 语义变更（重要）：**
- 切换到历史日期 = **严格单日查询** [该日, 该日]，绝不混入今天或其它日期标本；待排/采集实时队列仅今天视图拉取
- 自动审核跨午夜回溯仅在「查看今天」时生效（开启日~今天）；历史视图只审该日标本
- 回归：历史视图批审/F4 只审该日；切回今天后计数恢复正常

**8.8.11 新增回归点（本轮审查修复）：**
| 步骤 | 期望 |
|------|------|
| 开着日历弹层时关闭工作台 | 日历弹层一并收起，不再悬浮残留在 LIS 页面上 |
| 历史视图审过标本后切回今天 | 工作台切回今天的同刻，原生页面日期框恢复今天 |
| 详情面板开着时在搜索框打字 | 预热完成后焦点不被抢走，输入连续 |

---

## 13. 手工杂项排除 + 推送前沿即发（8.10.2 新增）

> 两件事：① 临检/免疫的「手工杂项」仪器分类**永不自动审核**；② 推送节奏从「尾沿静默等待」
> 改为「前沿即发 + 最短间隔」——单个单个做标本时每条即时推，连续做时按所选间隔合并一条。
> 弹窗里 `⏱ 连续结果合并推送` 的选项语义已改为「连续时最快 N 分钟一条」（取值不变）。

**手工杂项排除：**

| 步骤 | 期望 |
|------|------|
| 全部工作组 + 不勾任何仪器（=全选）开启自动审核 | 弹窗范围行末尾出现橙色「🔧 手工杂项不自动审核」提示；手工杂项下的待审标本**一直留着不被审掉**，其它仪器照常自动审 |
| 显式勾选「手工杂项」+ 其它仪器 | 同上：范围行有橙色提示；手工杂项标本不审，其它勾选仪器正常审 |
| 只勾「手工杂项」一台，开启自动审核 | 每轮无任何审核动作（空转，不弹小结 toast、不推送）；不报错、不刷屏 |
| 手工杂项标本 → 手动 Enter / F4 / 批审 | **照常可审**（排除只作用于自动审核，不碰手动路径） |
| 工作台分类计数 / 菜单栏读数 | 手工杂项标本仍照常计入「待审/不完整」等分类与仪器计数（显示口径不变） |
| 自动审核暂停推送（断流时） | 「当前拦下 N 例待人工」不再把手工杂项标本算进去 |

**推送节奏（建议在 `⏱` 选「连续时最快 2 分钟一条」下测）：**

| 步骤 | 期望 |
|------|------|
| 只做一个标本（前后 10 分钟无其它结果） | 该轮审完**立即收到推送**（不再等 60~90 秒）——这是本版最核心的变化 |
| 隔 6 分钟再做一个标本 | 又是立即推送一条（每条都走「前沿」） |
| 连续上机一批（结果不断出） | 第一条即时推；之后**约每 2 分钟一条合并小结**，不会每 30 秒响一次 |
| 一批做完（同批标本结果全出完） | 距上一条推送 ≥45 秒后发一条收尾小结，**不用等满 2 分钟**；不会和前沿那条紧跟着连响 |
| 危急值 / 堵孔0值 / 传染病阳性 / 心肌 / 负值 | 仍**立即** critical 推送（穿透勿扰），不受任何间隔限制 |
| `⏱` 选「不合并（每轮立即推）」 | 与旧版一致：每轮结算各推一条 |
| 合并推送正文 | 见下方 §14（8.10.3 起改为一标本一行的新排版） |
| 攒单期间整页刷新 / 重开工作台 | 攒着的内容不丢：重开工作台立即补发；即使**不重开工作台**，启动约 1 分钟后也会自动补发一条（不再躺到 30 分钟超龄被丢弃） |
| 免疫组慢项目（做很久才出结果） | 不会把推送无限期拖住：等不到就按所选间隔照常发（滞留超 20 分钟的标本不再算「在跑」） |

**回归关注点：**
1. §1 批审 + §2 F4 无回归（本版未改审核热路径）
2. 手机不再出现「30 秒响一次」的连响；也不再出现「单个标本等一分多钟才响」
3. 同一条推送不会重复发两遍（间隔地板 45 秒 + 60 秒同内容去重仍在）

---

## 14. 合并推送排版（8.10.3 新增；8.10.4 修订：概览进正文首行）

> 多个标本**合并成一条**推送（不是同时来几条），一条里怎么高效呈现多个标本：
> **一标本一行**「符号 流水号 项目组合　异常项+数值+方向」；**需人工的排最前面**且带参考范围；
> 已自动审的按仪器分组、不带参考范围；标本多时自动折叠成流水号列表。
> 仪器分布与核收时间区间放在**正文首行的「概览」**。
>
> **8.10.4 修订**：8.10.3 原计划用 Bark 副标题（subtitle）放概览，但**实测加密推送下 Bark
> 不还原 subtitle（明文才显示）**。本推送坚持端到端加密 → 放弃副标题，概览并入正文第一行。
> 因此不再需要网关机因副标题升级 relay 了（relay 照常转发 title/body）；`bark_relay.py`
> 带 subtitle 透传只是预留兼容，不影响加密推送。

**连通性自检（可选，确认网关 relay 在跑）：**

```bash
curl http://192.168.31.111:9111/notify_status -H "Origin: http://192.168.31.111:9111"
# 应返回 {"configured": true, "enabled": true, ...}
```

**排版（手机 + 手表）：**

| 步骤 | 期望 |
|------|------|
| 单个标本全正常 | 正文第 1 行 `生化 1 10:02`（概览，含仪器与核收时间），第 2 行 `✅ 全正常：流水号`；标题「🤖 已自动审 1 个标本」；**不出现**「━ 已自动审 ━」分区标题（≤3 个标本扁平模式） |
| 单个标本有异常 | 第 1 行概览，第 2 行 `⚠️ 流水号 项目组合　项目 数值🔺 …`；标题带「（异常 1）」 |
| 单个危急值留人工 | 第 1 行概览，第 2 行 `🚨 流水号 项目组合　项目 数值🔺🚨(参考范围)`；标题「🚨 危急值1 需人工」；critical 级 |
| 连续做一批（6~10 标本，含红线） | 一条推送内：首行概览 `生化 5 · 血细胞 2 · DXI800 1 09:14-09:25` → `━ 需人工 N ━` 区逐条带参考范围 → `━ 已自动审 N（异常 M） ━` → 按仪器分组`〔生化 异常 2〕`逐条（不带参考范围）→ 末行`✅ 全正常 N：流水号…` |
| 项目名 | 长中文名显示为通用缩写：甘油三酯→TG、高密度脂蛋白胆固醇→HDL、中性粒细胞绝对值→NEU#、丙氨酸氨基转移酶→ALT。未收录的名字原样显示（不出现看不懂的缩写） |
| 方向标记 | 升 🔺 / 降 🔽 / 0值 ⓿ / 危急 箭头+🚨，贴在数值后面 |
| 血常规 | RDW/比率/MPV 等低权重项仍省略，行尾 `+2` 表示略去数量；**危急值永不省略** |
| **需人工的血常规** | 拦下的理由必须看得见：即使全是低权重项也照常展开数值（不出现「⓿ 307 血常规 +2」这种看不到数值的行） |
| 高峰 20~40 标本 | 正文仍控制在 16 行左右：先砍每标本项目数 → 某仪器剩余标本折成`　…另 N 例：流水号…` → 整组折成`〔生化〕异常 7：流水号…`。**任何情况下流水号都在**，可在工作台按号查 |
| 一次拦下几十例（如整批堵孔） | 需人工逐条最多列 12 例，其余折成`　…另 N 例需人工：流水号…`；标题仍报完整例数 |
| **Apple Watch** | 标题 + 正文开头几行完整可读：首行概览（仪器分布+时间）+ 紧接着的需人工标本（红线优先排前的设计目的）；扁平模式下没有分区标题占位 |
| **推送加密开启时** | 全文进 AES 密文（Bark 云与 APNs 只见密文）；手机端 Bark 正常解密显示。**无副标题**（Bark 加密路径不还原 subtitle，属已知局限，已用正文首行概览替代） |
| 隐私 | 全文只有流水号/检验号、时间、项目组合、项目名与数值（需人工区另带参考范围）。**绝不出现**姓名/住院号/床号/科室 |

**回归关注点：**
1. `⏱` 选「不合并（每轮立即推）」时排版同样生效（单轮也走新排版）
2. 补报推送（页面刷新打断后的那条）标题末尾仍带「（补报）」，正文末行仍有补报说明
3. 断流「⚠️ 自动审核暂停」推送：正文首行 `当前拦下 N 例待人工`，随后逐条列出被拦标本
4. §1 批审 + §2 F4 无回归（本版只改推送呈现，未碰审核路径）

---

**不要用生产高峰大批量「赌」未测版本。** 审核路径有改动时，至少做完 §1 + §2 + §8（跨组）+ §10（自动审核）+ §11（堵孔0值）+ §12（日期选择）。

## 15. 轻微异常一键批审（8.12.0 新增）

> F4/横幅批审队列从「严格正常」扩容为「正常 + 轻微异常」：整管所有异常项目都在定稿轻微带内
> （甲类衍生项全放行、乙类按 ×ULN/×LLN 幅度带）且乙类 ≤4 项的 ABNORMAL 标本一并批审。
> **机器人零改动**：夜间自动审核异常逐条口径不变；分类桶不动，轻微异常在工作台仍显示为异常。
> 留痕：F4 放行的轻微异常写 `localStorage.LIS_MildAuditLog`（环形 500，不推送）。

| 步骤 | 期望 |
|------|------|
| 待审视图横幅（有轻微异常时） | 显示「✅正常 N · ⚠️异常 M · ⚡轻微 K」，按钮文案「⚡ 一键批审正常 N · 轻微 K · F4」；K=0 时文案与旧版一致 |
| 轻微异常标本在列表中 | 仍按异常行展示（分类桶未动），异常 chip 照常 |
| **F4 批审（含轻微异常）** | Toast「⚡ 开始批审 N 个正常 + K 个轻微异常」；轻微异常标本被一并审核、从异常视图消失 |
| 典型放行例 | WBC 10.8（≤1.2×ULN）、NEUT% 78、PLT 120↓（≥100）、ALT 58（≤1.2×ULN）、TG 2.4（≤1.5×ULN）、CK 330（≤2×ULN）、Ca 2.05↓、TSH 9.0、CRP 12（≤3×ULN）、PCT 0.4（≤0.5 绝对）、RDW-CV 16（甲类）等整管放行 |
| 典型拦截例 | WBC 12.5、PLT 95↓、K 5.65（绝对钳 5.6）、Cr 120（1.05×ULN）、cTnI 0.06、血锂/NH3/fPSA 任何异常、乙类 9 项、传染病历史不符、定性 flag A、负值、无参考范围——整管留人工，Enter 逐条仍可审 |
| **性激素六项性别化放行（8.15.0）** | 男=男性范围基准；女=各期汇总包络（各期下限取最低/上限取最高）基准；基准上乘倍数（H≤1.5×、L≥0.5×）。性别取不到 → 留人工。绝经后 FSH/LH 超包络 → 留人工（保守） |
| **同项目 #/% 合并计数** | NEUT# 6.9 + NEUT% 78 只计 1 项；甲类（RDW/MPV/PDW/PCT/P-LCR 等）不占名额 |
| 留痕日志 | 控制台 `JSON.parse(localStorage.LIS_MildAuditLog)` 可见本次放行明细（时间/流水号/项目/命中带）；机器人路径不写此日志 |
| **机器人回归（关键）** | 夜间自动审核行为完全不变：异常逐条照旧全放（红线除外）、正常批量只审严格 NORMAL、推送内容无差异 |
| 菜单栏（Mac） | `/stats` 新增 mildReady；有轻微异常可批时下拉多一行「└ 轻微可批 K」 |
| 紧急停用 | 代码内 `MILD_ALLOW_ENABLED = false` 即回到「只批审严格正常」（无需回滚版本） |

## 16. 白天/夜间方案 + 推送三档（8.13.0 新增）

> 🤖 弹窗新增「🔀 审核方案」：☀️白天（**8.15.5 起默认**，异常逐条仅限轻微带内，同 F4 批审口径）/ 🌙夜间（=旧行为，异常逐条全放、红线除外），
> **点选即生效**（跨刷新记忆，无需「更新并继续」）。正常批量两种方案下都只审严格 NORMAL，不受方案影响。

| 步骤 | 期望 |
|------|------|
| 🤖 弹窗「审核方案」 | 两个单选 ☀️白天 / 🌙夜间，**默认白天**（8.15.5 起；此前默认夜间，属最宽松口径）；点选立即 toast「已切换为白天/夜间方案」并持久化，无「更新并继续」要求 |
| 按钮状态（运行中） | 「🤖 自动审核(白天) mm:ss (全推)」/「(夜间)」；tooltip 含方案全称 |
| 切换即时生效 | 运行中切到白天 → 下一轮起超带异常（如 D-Dimer 2.74）不再代审，留人工原因「☀️ 白天方案留人工：D-二聚体测定 2.74 超出轻微带…」；切回夜间 → 恢复原全放行为 |
| 白天方案红线不变 | 危急/堵孔/待定/负值/传染病阳性/传染病历史不符/心肌危急线照旧拦，且归类优先于白天类 |
| 白天方案轻微带内照审 | ALT 58、NEUT% 78、PLT 120↓、整管乙类 ≤8 项等照常逐条审核 |
| 推送设置 | 只有三档：📲推送所有 / ⚠️只推送未成功 / 🔕不推送；「推送不成功+有异常标本」不再出现 |
| **旧设置迁移（一次性）** | 原存「推送不成功+有异常标本」的机器，升级后首次加载自动变「推送所有」，无需手动操作 |
| 白天留人工推送分组 | 标题/正文里超带留人工单独归类「白天留人工 N 例」，行首符号 ☀️，与危急/堵孔等红线区分 |
| 记录查看器 | 白天方案下超带标本进「留人工」记录，理由可见 |

## 17. 检视器患者信息补全（8.13.1）

> 待审视图右栏头部此前只显示 检验号/登记号/住院号/科室/仪器，申请医生因工作台行数据无此字段而缺失；
> 8.13.1 起从分类缓存带回的 LabInfo（GetReportInfoAll）补全，回退链与病人结果导出模块同源。

| 步骤 | 期望 |
|------|------|
| 待审视图右栏头部第二行 | 依次尽可能显示：检验号 · 登记号 · 住院号 · 床号 · 科室 · **申请医生** · **标本**（如 大便）· **流水号** · 仪器；无值的项自动隐藏 |
| 申请医生来源 | 行数据（Doctor/DoctorName/ReqDoctorName/ApplyDoctorName）→ 分类缓存 LabInfo（Doctor/DoctorName/ReqDoctorName/ApplyDoctorName），取到即显示 |
| 附加信息行（第三行） | 男/年龄 · 科室 · 住院号 · 床号 · 标本 · 申请医生 · 采集/接收/核收/审核 · 诊断；同样补 LabInfo 回退 |
| 详情面板附加信息行 | 同一函数驱动，医生字段回退链同步扩展，不回归 |

## 18. 规则表自检 + 批审汇总 + 留痕导出（8.15.5 新增）

> 三件事：① `MILD_ALLOW_RULES` 启动自检（防规则表被重排/放宽后**静默**改变放行口径）；
> ② 批审结束 toast 补上「跳过 N 条」；③ 轻微异常放行留痕可导出。

| 步骤 | 期望 |
|------|------|
| 控制台 `lisSelfTestMildRules()` | 返回空数组 `[]`（111 项全通过：正向 93 / 负向 10 / 编码 8）。返回非空数组即为**回归**，数组里每行写明「期望 X，实际 Y」 |
| 自检未通过时的表现 | 控制台一条 `console.warn`（列出所有不符项），**不阻断运行**；调试面板（`showDebugPanel()`）里也有记录 |
| 自检能否抓到回归（抽查） | 把「脂蛋白(a)」正则改回未锚定的 `脂蛋白[(（]?\s*a` → 应报「载脂蛋白A：不应命中，实际 LPA」；删掉「血小板压积」规则 → 应报「血小板压积：期望 a，实际 NONE」 |
| 批审结束 toast | 出现「批审结束（以列表为准）成功约 N」；**有跳过时补「，跳过 K 条（需人工过一遍）」**，且 toast 转 warning 色 |
| 无跳过时 | 不出现「跳过」字样，toast 仍为 success 色（不因这次改动而变色） |
| 控制台 `lisMildAuditLog()` | 返回 `LIS_MildAuditLog` 的数组（环形上限 500）；F4 放行过轻微异常后条数增加 |
| 控制台 `lisExportMildAuditLog()` | 下载 `LIS轻微异常放行留痕_YYYY-MM-DD.txt`，制表符分隔，含 时间/检验号/姓名/组合/仪器/乙类项数/命中项；留痕为空时返回「（轻微异常放行留痕为空）」不下载 |
| 自动审核日志未被污染（关键回归） | 跑一次 **F4 手动批审**（放行若干轻微异常）后，**不要**立刻开自动审核；先看「🕘 记录查看器」——手动批审的条目**不应**出现在自动审核日志里（留痕只进 `LIS_MildAuditLog`，不与自动审核累积器混流） |

## 19. 结果展示升级：微胶囊标签流（8.15.23 方案 A 完善）

> **彻底解决视觉疲劳 & 全端胶囊风格统一**：告别大面积高饱和满色底与整格厚重色块，行底色恢复纯白（大面积留白，不再有刺眼的黄蓝粉红斑马纹）。
> 所有测定结果统一采用**圆角微胶囊标签（Micro-Pill Tag）**，正常项也包裹于淡雅浅绿微胶囊中，整体视觉极其精致统合，单位置于胶囊外侧统一为柔和浅灰（`#888`）。
>
> | 档位 | 语义与流向 | 方案 A 视觉表现 |
> |---|---|---|
> | **正常项（风格统一）** | 正常参考范围内 | **浅薄荷淡绿底 + 细浅绿边 + 翠绿健康字**（bg:#f0fdf4;color:#15803d;border:1px solid #dcfce7），全端胶囊统合精致 |
> | **可批审·轻微偏高** | 轻微放行带内，会被 F4 / 自动审核放行 | **淡琥珀底 + 细金边 + 暖橙色字与细箭头** `18.4 ↑`（bg:#fffbeb;color:#c2410c;border:1px solid #fed7aa） |
> | **可批审·轻微偏低** | 轻微放行带内，会被 F4 / 自动审核放行 | **淡雾蓝底 + 细蓝边 + 宝蓝色字与细箭头** `6.8 ↓`（bg:#eff6ff;color:#1d4ed8;border:1px solid #bfdbfe） |
> | **须人工·显著偏高** | 超出放行带 / 未配规则，须人工复核 | **紧凑实色橙底微徽章 + 白字 + 实心小三角** `[ 42.8 ▲ ]`（bg:#ea580c;color:#fff） |
> | **须人工·显著偏低** | 超出放行带 / 未配规则，须人工复核 | **紧凑实色蓝底微徽章 + 白字 + 实心小倒三角** `[ 78 ▼ ]`（bg:#1d4ed8;color:#fff） |
> | **危急值** | 独立安全门，须原始 LIS 审核 | **醒目实色深红底微徽章 + 白字** `[ 6.6 ↑↑ 危急 ]`（bg:#b91c1c;color:#fff;font-weight:800），行带浅粉红微底 |
> | **堵孔 0 值** | 生化分析仪吸样堵孔误报 0 | **醒目实色紫底微徽章 + 白字** `[ 0 ⚠️ 堵孔 ]`（bg:#7c3aed;color:#fff），行带淡紫微底 |
> | **传染病特殊阳性** | HIV / 梅毒 / 丙肝等特异性阳性 | **金黄底深褐字微徽章** `[ + 阳性 ]`（bg:#fef08a;color:#713f12;border:1px solid #facc15） |
>
> ⚠️ **单位彻底分离**：单位（`mmol/L` 等）置于胶囊右侧单元格白底上，统一为 `#888` 浅灰，彻底消除单位与徽章底色冲突。
> 同一套微胶囊语言已同步到：详情抽屉结果表、右栏检视器结果表、待审卡片条目 chip、详情表历史小列（正常项浅绿底深绿字）、患者历史浮层（正常项浅绿微胶囊）。

| 步骤 | 期望 |
|------|------|
| **表格大面积纯白护眼（核心改善）** | 打开标本详情或看右栏检视器：普通高/低异常行不再刷整行黄/蓝背景，纯白底色干净清爽，鼠标悬停时有柔和浅灰蓝行高亮，长时间看完全不累眼 |
| **可批审项（轻微高/低）** | 测定值带有淡色微胶囊：偏高为淡琥珀底细金边配细箭头 `↑`，偏低为淡雾蓝底细蓝边配细箭头 `↓`，一眼看出是温和、可批审的轻微异常 |
| **须人工项（显著高/低）** | 测定值带有实心微徽章：显著偏高为实色橙底白字实心三角 `▲`，显著偏低为实色蓝底白字实心倒三角 `▼`，形态与轻微项截然不同，一眼定位需重点关注的重度异常 |
| **危急值与堵孔** | 危急值为实色红底微徽章 `↑↑ 危急`，且整行带浅粉红底色；生化堵孔为实色紫底微徽章 `⚠️ 堵孔`，且整行带淡紫底色 |
| **单位清晰可见** | 胶囊外的单位（如 `g/L`、`10^9/L`）为整齐的小号浅灰字，在纯白底上非常清晰，不会出现字色与底色看不清的情况 |
| **状态提示挂在 title** | 鼠标悬停在异常胶囊单元格上：仍能看到气泡提示「↑ 高（轻微带内，F4 可批审）」、「▲ 显著偏高（超带，须人工审核）」等 |
| **历史 3 次小列微胶囊化** | 历史列数值采用同款微胶囊标签，轻微异常与显著异常按当前规则同步上色 |
| **患者历史浮层微胶囊化** | 点击 🔎 打开历史浮层，历次结果同样展示为圆角微胶囊标签，视觉一致 |
| **规则调整（⚙）实时联动** | 点 ⚙ 放宽某个项目的上下限保存后，结果表和待审卡片上的实色橙徽章立即变为淡琥珀镂空微胶囊，按 F4 即可批审 |

## 20. 待审卡片配色换代（8.15.8）

> 起因：卡片级原用「蓝＝可批审 / 橙＝不可批审」（**可批审性**轴），而条目级结果色用「橙＝升高 / 蓝＝降低」
> （**方向**轴）——同一色相在两个层级含义相反。现卡片级改走**绿系 + 紫**，与条目级的橙/蓝彻底不重叠。
> 配色：正常 `#34d399` 浅薄荷 / 可批审 `#047857` 深翡翠 / 需人工 `#5b21b6` 深紫 / 危急 `#dc2626`（未改）。
> 左边条 + 底色两个线索都换（用户确认保留现状形式）。横幅计数同步：`#047857` / `#5b21b6`。

| 步骤 | 期望 |
|------|------|
| 全部正常的标本卡 | 左边条为**浅薄荷绿**（`#34d399`）、底色近白（`#fbfefc`）——四档里最浅 |
| 有轻微异常可批审的卡 | 左边条为**深翡翠绿**（`#047857`）、底色 `#ecfdf5`——**明显比正常深**，两档一眼可辨 |
| 超带需人工的卡 | 左边条为**深紫**（`#5b21b6`）、底色 `#f5f3ff`；**不再出现橙色边条** |
| 危急卡 | 红色不变（`#dc2626` / `#fef2f2`） |
| 卡片内条目 chip | 仍为条目级橙/蓝浅深分档（§19），**与卡片色不再撞色** |
| 横幅计数 | 「✅ 正常」绿、「⚡ 轻微」深绿、「⚠️ 异常」**紫**、「🚨 危急」红——不再有蓝色计数 |
| ⚡ 徽章 | 「轻微可批」的 ⚡ 为深绿（原蓝） |
| 键盘上下切换（focused） | 聚焦态边框/底色用各自状态色，**不出现蓝色聚焦环** |
| 悬停卡片 | 边框变中性灰，不出现蓝色 |
| 堵孔/传染病警示 | 仍为琥珀色覆盖（`has-zero` / `has-infection-warning`），未受影响 |
| 远距离扫读（关键） | 站在 1 米外看列表：**浅绿=不用管、深绿=可按 F4、紫=要人工看、红=危急** 四档能分清 |

## 21. 轻微放行范围自助调整（8.15.9 新增）

> 详情面板结果表**项目名后面**的 ⚙：调该项目的 F4/自动审核放行倍数、给未配规则的项目新增规则、
> 停用某条规则、恢复默认。改动存 `LIS_MildRuleOverrides`，**默认无覆盖时行为与之前完全一致**。
> ⚠️ 这是**医疗判定参数**：放宽后该项目异常值会被自动放行，不再人工复核。改动全部写入留痕。
> **8.15.16**：放宽**不再弹「确认放宽？」对话框**——「放宽」只体现在保存后的**提示颜色**上
> （黄＝放宽 / 绿＝普通保存），判定口径与留痕机制均未改动。

| 步骤 | 期望 |
|------|------|
| 详情面板结果表（8.15.25 调优） | 项目名后面配极简微型齿轮，鼠标悬停该行时平滑淡现（平时隐身），绝不折行占多余行高；已人工改动的项目常显暖金小标；结果表格与微胶囊字号适度调大（12.5px~13px），行高保持在 22~23px 紧凑舒适区间，兼顾清晰易读与免滚动一屏看全 |
| **待审界面右栏检视器（8.15.10）** | 右栏结果表**同样**有 ⚙，点开行为与详情抽屉完全一致 |
| **右栏保存后实时生效（8.15.10 关键）** | 在**右栏**改完倍数保存 → 右栏**立刻**重渲染（出现短暂「正在加载结果…」再刷出新配色），无需切标本或刷新页面；待审列表的卡片配色也随之更新 |
| 详情抽屉保存后实时生效 | 在详情抽屉改完保存 → 抽屉立刻重渲染；若同时开着右栏，右栏也一起更新 |
| 两个入口不会重复弹窗 | 先在详情抽屉点 ⚙ 关掉，再在右栏点 ⚙ —— 只出现一个对话框（旧的会被移除） |
| 点已配规则的项目（如 WBC）的 ⚙ | 弹出对话框：显示「本标本参考范围 4.0 ~ 10.0」、偏高倍数 `1.2`、偏低倍数 `0.85`，右侧实时显示「→ 放行至 12」「→ 放行至 3.4」 |
| 改倍数时 | 右侧预览**实时跟着变**；清空某一方向 → 该方向显示「→ 不放行」 |
| 点未配规则的项目（如某新项目）的 ⚙ | 弹「该项目当前未配放行规则」+ 规则类型单选（乙类按倍数 / 甲类全放行），选甲类时数值输入框消失 |
| **新增规则时也要显示参考范围与实时预览（8.15.12 关键）** | 点**未配规则**项目的 ⚙ → 应显示「本标本参考范围：X ~ Y」，且填倍数后右侧出现「→ 放行至 Z」。**若显示「取不到」，看它是哪一种**：<br>· 「参考范围文本「…」未能解析出数值上下限」→ 数据里有范围但格式没认出来，把该文本发我补解析<br>· 「本标本数据里没有参考范围字段」→ LIS 没给范围 |
| 输入框文字完整 | 输入框占位符（8.15.15 起为「放行至」/「倍数」）**完整显示**，不被截断 |
| **改为填绝对值（8.15.14）** | 输入框里填**绝对值**（如 12.50），单位显示在右边；**后面自动算出倍数**（如「＝ 1.25× 上限」） |
| **输入框无上下箭头（8.15.15）** | 输入框**没有**数字增减箭头；可以用退格/删除键**正常清空**、可以选中替换、可以随便改 |
| **每个方向单独三选一（8.15.15）** | 偏高、偏低各有一个下拉框：**不放行 / 按数值放行 / 不拦截**。选「不放行」或「不拦截」时右边的输入框变灰不可填 |
| 三态含义 | **不放行**＝该方向异常一律留人工（最严）；**按数值放行**＝放行到填的值；**不拦截**＝该方向一律放行（最松） |
| 两个方向互相独立 | 可以「偏高不拦截 + 偏低不放行」这类组合，互不影响 |
| **新增规则默认值＝参考上下限（8.15.15）** | 给未配规则的项目新增时，偏高默认填**参考上限**、偏低默认填**参考下限**（即倍数 1×，等于还没放宽）；在这个基线上往上调 |
| 默认值不算放宽 | 新增时若保持默认（倍数 1×）直接保存 → 提示为**绿色 success**（不出现「已放宽」的黄色 warning）——只放到参考限本身，没多放任何东西 |
| 切换下拉不丢输入 | 选了「按数值放行」填了一半，临时切到「不放行」再切回来 → **已填的值还在**（不重渲染） |
| 两个方向都不放行 | 会提示「这条规则没有意义；请至少放行一个方向，或勾选整条规则停用」 |
| 从「不拦截」改成数值 | 属于**收紧**，提示应为**绿色 success**（若给成黄色 warning 说明判定口径被改坏了） |
| 倍数随输入实时更新 | 改绝对值 → 后面的倍数立刻跟着变 |
| **不动直接保存不漂移（关键）** | 打开对话框**什么都不改**直接保存 → 该规则的倍数**必须原样不变**。若变了说明防漂移失效（绝对值只显示 2 位小数，来回换算会漂移：9.8×1.03 显示 10.09，除回去是 1.0296） |
| 越界提示带具体数值 | 偏高填得比参考上限还低 → 提示「不能低于参考上限 12.1（那样等于不放行），请留空」 |
| 无参考范围时退化为倍数 | 该方向取不到参考范围时，标签自动变回「偏高放行倍数」，并在上方说明原因——**不会因为没范围就设不了** |
| 单侧范围 | 如参考范围是 `<0.55`（只有上限）→ 偏高按绝对值、偏低按倍数，两侧各自独立判断 |
| **弹窗可拖动（8.15.13）** | 按住标题栏「⚙ 轻微放行范围」拖动 → 弹窗跟着走；拖到屏幕边缘会被夹住，**不会拖出屏幕找不回来** |
| **背后结果表不被遮挡（8.15.13 关键）** | 弹窗打开时**没有全屏深色遮罩**；背后的结果表**清晰可见**，且**可以滚动、可以点击**（弹窗只在自身范围内挡鼠标） |
| 拖动位置被记住 | 拖到某个位置关掉，再打开 → 弹窗出现在**上次的位置**（存 localStorage `lis-mild-dlg-pos`） |
| 位置兜底 | 把浏览器窗口缩小后打开 → 弹窗仍在视口内（旧坐标会被夹回） |
| 关闭方式 | ✕ / 取消 / Esc 均可关闭；**点弹窗外不再关闭**（有意为之——否则滚表格时会误关） |
| 关闭按钮不误触拖动 | 直接点 ✕ 是关闭，不会变成「开始拖动」 |
| 倍数越界 | 偏高倍数填 `0.9`（小于 1）→ 提示「不能小于 1，请留空」并**不保存**；偏低倍数填 `1.5` → 同样拦下 |
| 两个方向都留空且未勾停用 | 提示「请至少填一个方向，或勾选一律不放行」 |
| **放宽不弹确认框（8.15.16 关键）** | 把 WBC 偏高从 1.2 改到 1.5 → 保存**不再弹 `confirm`**，直接保存成功并关闭；提示是**黄色 warning**「已放宽放行范围并立即生效 —— 该方向异常结果不再人工复核（改动已记留痕）」 |
| 只是收紧 | 把 1.2 改到 1.05 → 直接保存，提示是**绿色 success**「已保存，立即生效」（文案不带「放宽」二字） |
| **放宽判定口径未变（回归）** | 放宽/收紧的**判定**逻辑一行未改，只换了承载方式（弹窗→提示）。`HANDTEST` 的口径与 8.15.15 完全一致：默认不限→给数值＝收紧、默认「不放行」→给数值＝放宽、新增倍数恰 1×＝不算放宽 |
| 放宽仍留痕 | 上一步放宽后，控制台 `lisMildRuleLog()` 仍能看到这条改动（去掉弹窗**不影响**留痕） |
| 弹窗内警示仍在 | 打开 ⚙ 时，乙类下方那段黄底警示（「设为「按数值放行」或「不拦截」后…不再人工复核」）仍然显示——风险说明没被一起删掉 |
| 保存后立即生效 | 对话框关闭，详情面板**自动重渲染**（不用手动刷新）；待审列表也会刷新 |
| ⚙ 状态可见 | 改过的项目 ⚙ 变**橙色**，悬停 title 带「（已人工修改）」；停用的带「（当前已停用）」 |
| 恢复默认 | 对话框「恢复默认」→ 该规则回到默认值，⚙ 橙色消失 |
| **审核真的跟着变（关键）** | 把 WBC 偏高倍数放宽到 1.5 → 找一条 WBC 在 1.2~1.5×ULN 之间的标本 → 该值应由**满色橙底白字**变为**白底橙字＋实线竖条**（8.15.19 口径），且按 F4 **确实能批掉**（改之前是被拦下的）。**改完还是被拦 = 覆盖没生效** |
| 停用规则（一律不放行） | 勾上后该项目**任何**异常都让整管留人工；F4 跳过的原因里能看到「已被手动停用」 |
| **分男女项目的停用（8.16.2 修复）** | 对性激素六项（FSH/LH/PRL/E2/PROG/TSTO）或 RBC/HGB/HCT/GGT/CR/UA/CK/FER 任一项目：⚙ 勾「整条规则停用」→ 保存 → **重开 ⚙ 复选框应为勾选态**，且该管异常被 F4 拦下。8.16.2 之前这批项目的 off 根本存不进覆盖层，勾了等于没勾（仍在按原带自动放行） |
| 分男女项目切 tab 保留勾选（8.16.2） | 在 ♂男性设置 勾上「整条规则停用」→ 切到 ♀女性设置 → 再切回男性 → 勾选**仍在** |
| BAS 两条规则互不牵连（8.16.2） | 对「嗜碱性粒细胞绝对值」⚙ 改放行上限 → 保存后「嗜碱性粒细胞百分比」的上限**不变**（仍为 2）；反向亦然 |
| **女患者标本上保存必须真的生效（8.16.5 关键）** | 找一条**女性患者**的标本（这条是前提！男患者标本上不会暴露问题），对任意**非分男女**项目（WBC/ALT/球蛋白/嗜酸性粒细胞…）点 ⚙ → 改一个方向的值 → 保存 → **重新点开 ⚙，必须显示刚改的值**。8.16.5 之前这里界面读的是 `state.f`、保存却读 `state.m`，女患者标本上「改完保存」写回的是**打开弹窗时的旧值**（界面还提示保存成功），甲类→乙类更会直接报「两个方向都选了不放行」存不进去 |
| 同上 · 甲类切乙类 | 女患者标本上给「球蛋白」切乙类、把偏高改成 40 → 保存 → 重开应显示 **40**（不是 42）；男患者标本上重复一遍也应一致 |
| 同上 · 分男女规则 | 女患者标本上给 RBC/性激素六项改「♀ 女性设置」→ 保存 → 重开仍在女性 tab 且显示新值；男性 tab 的值**不受影响** |
| **甲类 ⇄ 乙类 自由切换（8.16.4 关键）** | 点**甲类项**（红细胞分布宽度 / 平均血小板体积 / 球蛋白 / 白球比 / 中性粒细胞百分比 / 肾小球滤过率…）的 ⚙ → 弹窗顶部**有「规则类型」单选**且「甲类（全放行）」为选中态；改选「乙类（按数值）」→ **立即出现**偏高/偏低两个方向的「不放行 / 按数值放行 / 不拦截」下拉 + 数值框 + 参考范围提示，并按 1.2× / 0.8× 预填（可改）。保存后 ⚙ 变橙，该管按新带判定 |
| 甲类 → 乙类 确实收紧 | 「球蛋白」切乙类保存（参考 20~35 → 上限 42）→ 找一条球蛋白 45 的标本，F4 应**留人工**（切之前甲类是全放行）；同一管把值看成 40（≤42）则可批掉 |
| 乙类 → 甲类 也能切 | 对 WBC 点 ⚙ 改选「甲类（全放行）」→ 保存提示**黄色**「已切换为甲类（全放行）…」；此前被乙类带拦下的 WBC 12（>11.4）此时可被 F4 批掉（**危急值/红线照旧拦**） |
| 来回切不丢数值 | 甲类 → 乙类（手改数值）→ 切回甲类 → 再切乙类：**刚才手改的数值仍在**，不会被 1.2/0.8 预填覆盖 |
| 恢复默认后类别也复原 | 「球蛋白」切乙类保存 → 重开 ⚙ 点「恢复默认」→ 再重开：单选回到**甲类**（不能永久停在乙类） |
| 切换类别后自检不误报 | 把任一甲类项切成乙类后，控制台 `lisSelfTestMildRules()` 仍返回 `[]`（自检断言看默认类别，不该因人工切换报警） |
| 类别标签随单选变化 | 弹窗标题下的「项目名 · 甲类（全放行）/ 乙类（按数值）」随单选实时变化；与默认类别不同时该标签为**橙色** |
| 甲类项转乙类后占名额 | 甲类项切成乙类后，它开始计入「整管乙类 ≤8 项」——同管 8 个乙类 + 1 个刚转乙类的项目 = 9 项 → F4 应留人工 |
| 红线不受影响（关键） | 放宽 WBC 到 10× 后，危急值/堵孔0值/梅毒丙肝HIV阳性/心肌危急线**照旧拦**，不会因放宽而被放行 |
| 新增规则只匹配该名 | 给「某项目」新增规则后，**名称相近的其它项目**（如「某项目B」「某项目测定」）不应被误放行 |
| 留痕 | 控制台 `lisMildRuleLog()` 输出制表符分隔的变更记录（时间/项目/从什么改成什么/改成什么）；`lisMildRuleOverrides()` 看当前全部覆盖 |
| 一把恢复 | 控制台 `lisResetMildRuleOverrides()` → 全部回到默认，界面刷新后颜色/判定同步复原 |
| 规则表自检 | 改动后 `lisSelfTestMildRules()` 仍返回 `[]`（覆盖层不应破坏 111 项匹配用例） |
| 多机同步（8.16.0 起） | 覆盖存 localStorage **并经网关广播**——任一台电脑改完即同步全科室；离线时的改动会在下次启动/切回窗口时补推。旧版「只对本机生效」的说法已作废 |

## 21.5 自动审核记录：三色分层 + 异常项英文简称（8.16.8）

> 工作台右上 🕘「自动审核记录」。原来三种结论的徽章都是**浅底深字**、与卡片底几乎同色，
> 扫一眼分不出「过了还是没过」；异常项又是全中文长名，一行挤成一团。

| 步骤 | 期望 |
|------|------|
| **三色分层（关键）** | 卡片按审核结论着色，三处同时上色（左侧 4px 色条 + 极淡底 + 实心徽章）：<br>· **绿** = ✓ 正常通过（全正常通过自动审核）<br>· **蓝** = ⚡ 轻度异常通过（有轻微异常但已放行）<br>· **橙** = ⏭ 未通过（留人工） |
| 徽章可读性 | 徽章是**实心底 + 白字**，不是原来的浅底深字；不用逐字读也能看出结论 |
| **异常项英文简称（关键）** | 轻度异常通过卡片的异常项显示为 **WBC / NEU# / HGB / PLT / ALT / TG / CRP** 这类规范简称（与工作台卡片、Bark 推送同一套字典），不再是一长串中文 |
| 简称悬停 | 悬停任一异常 chip → tooltip 显示**中文全称 + 结果 + 单位 + 偏高/偏低** |
| 顶部概要统计条 | 「✓ 正常通过 N · ⚡ 轻度异常通过 N · ⏭ 未通过 N」三色与卡片一致 |
| 未通过卡片 | 橙色左边条 + 橙色徽章 + 原因框（「梅毒/丙肝/HIV 阳性，留人工复核」）颜色协调，不再是红徽章配黄框 |
| 展开卡片 | 点卡片展开「完整指标结果」→ 表格里仍是**中文全称**（明细视图不简化），且左侧状态色条在展开后**不被青色边框覆盖** |
| 搜索仍可用 | 搜索框按**中文全称**搜（如「中性粒细胞」）仍能命中——日志里存的是全称，简称只在渲染时替换 |
| **四列对齐（8.16.9 关键）** | 竖着扫下来，「徽章 / 姓名 / 检验号 / 组合项目」四列必须落在**同一条竖线**上。此前徽章宽度随文案长短变（轻度异常通过 7 字 / 正常通过 5 字 / 未通过 4 字），后面三列跟着左右参差 |
| 徽章文字对齐 | 三个徽章的**文字起点**也要对齐（定宽徽章 + 固定宽度图标位 + 左对齐；居中会让文字起点随文案长短浮动，等于没对齐） |
| 长姓名不破列 | 姓名超 4 字时**省略号截断**（悬停看全名），不能把后面的检验号列推歪 |
| 检验号位数不同 | 12 位 / 13 位检验号都**居中**在同一列宽里，组合项目列不被推歪 |
| 「N 项异常」计数 chip | `1 项` 与 `12 项` 宽度一致，后面的异常项 chip 起点对齐 |

## 21.6 历史值也必须按「危急」着色（8.16.10）

> 现场：详情面板历史列里，**本次**低钠 119.7 是深红「危急」，而**历史**的 113.8 / 112.5
> 却只是普通蓝色低值 —— 同一个项目自相矛盾。根因：LIS 历史快照（PreResult）只带 H/L
> 级别标志，历史值从未与**危急范围**比对过。

| 步骤 | 期望 |
|------|------|
| **历史列危急着色（关键）** | 打开一条钠（参考 135-155）的标本，历史列里 113.8 / 112.5 这类**低于危急线**的值，应与本次值一样是**深红底白字**，不是蓝色 |
| 与本次一致 | 同一行「本次」与「历史」的危急着色口径必须一致——本次红、历史也红 |
| 未配危急值的项目 | LIS 未配危急范围的项目（如 DXI800 的 cTnI/CK-MB/MYO）历史列**保持原样**，不误染红 |
| 临界附近不算危急 | 危急上限 6.2 时，历史钾 6.1 仍是橙色「高」，只有 ≥6.2 才红 |
| **历史浮层（🔎）的危急值（关键）** | 打开历史浮层 → 真危急值（标志 ↑↑/↓↓）的**结果格必须是深红底**。8.16.10 之前 `.hist-val.critical` 这个类**根本没写 CSS**（注释写着「危急固定满色红底」但类不存在），真危急被画成了普通满色橙/蓝，和普通高/低分不出来 |
| 历史浮层未配危急值 | 该次报告没有危急范围时保持原样，不误染红 |

## 21.7 「已知晓」不重复推送 + 不再强行拉回工作台（8.16.11）

> 现场两个问题：① 开自动审核+推送后，每次退出工作台再进去，待审里的危急值标本又被推一遍；
> ② 开自动审核时回原始 LIS 看东西，会被强行切回工作台。

| 步骤 | 期望 |
|------|------|
| **「已知晓」按钮出现范围** | 待审里**危急值**（🚨）和**超带无法自动审核**（⚠️）的卡片，姓名/门诊徽章后面出现灰色「已知晓」按钮；正常/轻微可批的卡片**没有**（它们本来就能批审，不需要这个） |
| 点击「已知晓」 | 变绿色「✓ 已知晓」+ 提示「不再进自动审核推送（仍留在待审，仍须人工审核）」 |
| **点完不再重复推（关键）** | 开着自动审核+推送：标记后退出工作台再进去（乃至刷新页面）→ 该标本**不再出现在手机推送里**；同一批其它未标记的照旧推 |
| 可取消 | 再点一次绿色「✓ 已知晓」→ 回到灰色；**不用刷新页面**，下一轮自动审核就会重新推它（取消时会清掉「同标本同原因只记一次」的记忆） |
| 控制台兜底 | `lisAAckList()` 列出当前所有「已知晓」标记（标本号 + 时间）；`lisAAckClear()` 清空全部、`lisAAckClear('标本号')` 清单个 |
| **安全不受影响（关键）** | 标记后：卡片**照旧是红色/紫色**、照旧留在待审、照旧不能被 F4/自动审核放行——「已知晓」只掐推送，不放松审核 |
| 跨页存续 | 标记后刷新页面/重开工作台，按钮仍是绿色（存的是标本 ReportDR） |
| 7 天后自动清 | 已处理的标本不会无限堆积（存储卫生）；不会让老标本复活重推 |
| **回原始 LIS 不被拉回（关键）** | 开着自动审核时按 Esc / 点退出工作台回到原始 LIS → **停在那里不动**（等 30s 以上也不弹回）。此前自动审核每轮保活会把它强行重开 |
| 自动审核仍在跑 | 待在工作台外时，自动审核**照常继续**（审核走 iframe，不需要工作台可见）；回工作台看日志仍能看到这期间审掉的标本 |
| 主动打开即恢复 | 自己点开工作台（悬浮钮/任意入口）→ 保活恢复正常，之后刷新页面仍会自动重开 |
| 整页刷新后的自动重开 | 未主动退出过工作台时，整页刷新后仍会自动重开（原有行为不变） |

## 21.8 批审切组死循环必须能停下来（8.16.15）

> **现场 bug（必测）**：开自动审核时，工作台在「原生 LIS ↔ 工作台」之间不停来回切，
> 反复提示「切换工作组后继续批审」，伴随审核失败；**手动关掉自动审核也停不下来**，持续很久。
> 根因：跨组标本切不过去时（无权限 / 原生切组失败）批审队列无限自我续跑，且
> `ensureAuditQueueWorkGroup` 在批审 while 循环**之前** `return false`，
> 绕过了循环开头的「自动审核已关闭就中止」检查；全链路又没有切组次数上限，
> 而 `_writeFullQueue` 每次刷新 `time`，让队列的 10 分钟过期守卫也永不触发。

| 步骤 | 期望 |
|------|------|
| **关掉自动审核必须立刻停（关键）** | 批审正在跨组卡住时点「关闭自动审核」→ **立刻停止**，不再切组、不再重载；提示「⛔ 批审已中止：自动审核已关闭，剩余 N 例请人工处理」（红色） |
| **卡住后刷新页面不复活（关键）** | 关掉自动审核后刷新页面 → 不会又自动续跑切组（此前会：队列 pausedForSwitch 落盘，每次重载都续跑） |
| 切组不上时到上限即停 | 若某工作组确实切不过去（账号无权限等）：最多连续尝试 3 次 → 中止并提示「已连续尝试切换工作组 N 次仍未成功（可能当前账号无该工作组权限，或原生切组失败）」，**不再无限重载** |
| 中止原因要具体 | 提示说明是「切不过去」，不能笼统报「审核失败」误导排查方向 |
| **正常多组批审不受影响（关键）** | 待审里有 A/B/C 三个工作组的标本：自动审核照常来回切换、全部审完，**不会被中断或熔断**（切组成功即归零，只在「连着切不过去」时才计数） |
| 熔断期只审当前组 | 触发中止后 10 分钟内：自动审核只审当前登录工作组的标本；跨组的记为「留人工」并注明「切换工作组多次未成功，已暂缓跨组批审」，**不静默消失** |
| 手动 F4 批审同样有上限 | 手动批审遇到切不过去的跨组标本也会在 4 次后中止（防手动路径死循环） |
| 剩余标本不丢 | 中止只是停止批审，标本**照旧留在待审视图**，可人工或 F4 处理；提示里给出剩余例数 |
| 控制台手动清残留 | 万一仍有残留卡死，控制台执行 `localStorage.removeItem('LIS_AuditQueue'); localStorage.removeItem('LIS_AA_StuckSwitch'); localStorage.removeItem('LIS_AA_SwitchFuse');` 后刷新 |


## 21.9 跨组审核优先「免切组」，且不确定就不切组（8.16.16）

> **设计前提**：跨组审核**本来就不该回原生 LIS 切换工作组**。原生报告页支持按
> `WorkGroupMachineDR` 直接查工作列表（`ShowWorkList`），所以批审对跨组标本的做法是
> **先试免切组**，只有「原生列表里确实选不到」才回退切组。
> 8.16.16 修的是「本该免切组的场景退化成切组」的两个入口。

| 步骤 | 期望 |
|------|------|
| **正常跨组批审不切组（关键）** | 当前登录临检组，待审里有免疫组标本 → 批审**不整页重载**，直接审掉；进度条不出现「切换到XX继续批审」 |
| 免切组不再依赖机台号 | 标本没有机台 DR（如手工录入）时也会先尝试一次免切组查询，**不再直接切组** |
| 切组只作最后手段 | 只有免切组确实选不到该行时才切组；切组后仍选不到 → 4 次上限后中止（见 §21.8） |
| **当前组读不到时不切组（关键）** | 若原生下拉框 `sl_changeworkgroup` 与 LIS 全局变量都读不到（页面异常/结构变化）：批审**不切组、不整页重载**，改为按免切组处理；该标本选不到则放回队尾（最多 3 次）后记「留人工」 |
| 不会误判为跨组 | 工作台视图选着 A 组、实际登录 B 组时，**不会**因为视图过滤器把 B 组标本当成跨组去切 |
| 控制台可查证 | 出现上述情况时控制台有 `[批审] 取不到权威登录工作组 → 跳过跨组判定，按免切组处理: <标本DR>`，据此判断是不是原生页面结构变了 |
| 审完不误切回 | 起始组读不到时不记录 origin → 批审结束**不会**切回一个错误的组 |
| 安全边界不变 | 无论走哪条路径，**看不到结果绝不点审核**；选不到的标本一律留人工，不静默丢弃 |


## 21.10 切组中止彻底闭环 + 熔断权威组 + 轻微放行清空同步（8.16.17）

> 针对 8.16.15/8.16.16 复查发现的三个深层暗病彻底闭环：
> ① 守卫中止后立即置 `_batchAbort = true`，掐死 `finally` 重新回写已删队列、阻止误入补审轮次，且保留红色中止 Toast；
> ② 熔断期过滤工作组改用 `resolveLoginWGReliable()` 权威登录组，不在工作台切标签查看时误把真实登录组标本过滤掉；
> ③ 轻微放行规则同步消除「清空/恢复默认导致旧电脑把老数据作为种子重新复活网关」的时序漏洞。

| 步骤 | 期望 |
|------|------|
| **切组中止后绝不误入补审（关键）** | 触发连续切组失败或标本卡住中止时 → **红色中止 Toast 稳定保持**，不会被后续的「❌ 审核全部失败」或「批审完成」冲掉；不会把未成功的标本拿去重新补审一遍 |
| **中止后队列不被救活（关键）** | 守卫中止整批后，刷新页面**不会死灰复燃**（`_writeFullQueue` / `_writeQueueProgress` 在中止态下拒写本地存储） |
| **熔断期工作组准确（关键）** | 熔断期内：工作台即使切换到其它工作组查看，自动审核仍按**实际登录工作组**执行过滤与审核，不会把真实登录组的标本过滤掉 |
| **轻微放行清空能同步（关键）** | 在某台电脑上将某规则（或所有规则）点击「恢复默认」清空后 → 其它电脑拉取同步时**能正常同步清空**，绝不会把其它电脑本地的旧规则重新当作种子灌回网关 |



## 21.11 跨组批审不切组（8.16.24 回归修复）

> **现场原话**：「14 号及之前的标本审报告是不切组的，就这两天出的问题。」
> **根因（已定位到具体提交）**：`7d3664b` = **8.15.27（09-14 15:34）**
> 「perf(audit): 优化跨工作组批审切换速度，消除跨组选行 4.5s 超时死等」——
> 它把两处「先试免切组」改成了「立即切组」：
> ① `ensureAuditQueueWorkGroup` 原本是
>    `// 8.5.10: 跨组先试不切组……不再预先切组；主循环内选不到标本时才回退切组` + `return true`，
>    被改成「不在当前原生列表 → 立即切组」——判据只是**一次** `selectNativeRowByReportDR`，
>    而批审刚启动时原生列表还停在默认日期/上一个机台，这一步几乎必然落空；
> ② 主循环原本走**完整选行**（`waitAndSelectNativeRow`：轮询等待列表返回 + FindFast 条码兜底
>    + 按机台/日期刷新），被换成「快速探测（`fast:true` **只等 70ms**）失败 → 立即切组」——
>    而 `ShowWorkList` 是异步查询，70ms 远未返回，探测也几乎必然落空。
> 两处叠加 ⇒ 每条跨组标本都白切一趟组：整页重载 + 重新 CA 认证
> （现场那个「是否记住密码」弹窗）+ 工作台重建。

| 步骤 | 期望 |
|------|------|
| **跨组标本批审不整页重载（关键）** | 当前登录生化组、待审里有临检/免疫组标本 → F4 批审**不整页重载**、不出现「切换到XX继续批审」；原生页面始终停在工作台后面 |
| **单审一条跨组标本也不重载（关键）** | 待审里只有 1 条跨组标本时按 F4 → 同样**不整页重载** |
| 不再反复弹「是否记住密码」 | 不重载就不会重新 CA 认证，浏览器密码气泡不再反复出现 |
| 跨组选行会等几秒（有意） | 跨组标本在当前组原生列表里最多等 2~4.5s（轮询 + FindFast），选中后正常审核——**宁可等几秒，也不白切一趟组** |
| 真的选不到才切组 | 完整选行（含回退切仪器重试）都失败后才由兜底分支切组；仍有 `aaQueueGuardSwitch` 守卫 + 次数上限，不会死循环 |
| 起点绝不切组 | `ensureAuditQueueWorkGroup` 内不出现 `safeSwitchWG` / `pausedForSwitch`（回归测试 A16 守住这条） |
| 安全边界不变 | 「看不到结果绝不点审核」；选不到的标本仍放回队尾（≤3 次）→ 留人工 |
| **跨组标本不会「假成功」（关键，8.16.25）** | 批审跨组标本后刷新工作台：**提示成功的条数 = 实际审掉的条数**。若某条其实没审掉，应显示为「跳过 / 留人工」，**不得**报成功（此前会「提示两条成功、刷新后留一条」＝静默漏审） |
| 判定变严的代价（有意） | 跨组标本审核后的确认会比同组略慢（要等原生状态回写 / wsData 刷新才认成功）——**宁可慢，也不把没审掉的记成已审** |
| 同组标本行为不变 | 同组标本的「审核后行消失＝成功」快路径**保持原样**，批审速度不受影响 |


## 21.12 批审队列上下文 / 补审未完成 / 锁归属 / 确认窗收窄（8.16.31）

> 离线回归：`node audit_queue_context_test.mjs`（86 断言，G1~G6 五组缺陷 + 静态不变量）、
> `node aa_switch_guard_test.mjs`（62 断言）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node audit_queue_context_test.mjs` → 必须 **36 项失败**；
> `LIS_SRC=/tmp/old.user.js node aa_switch_guard_test.mjs` → 必须 **2 项失败**。

| 步骤 | 期望 |
|------|------|
| **跨整页刷新后续跑不漏审（关键）** | 批审中手动刷新页面（或切组重载后自动续跑）：剩余标本应**逐条正常审掉**，不得出现一批「待定标本不可自动审核 / 分类明细未获取」留人工——此前 wsData 尚空时查不到明细，会恒判 UNCERTAIN 且补审不收 |
| 分类明细拉取失败是**技术**失败 | 网络瞬断时该条放回队尾重试（≤3 次），日志显示「详情结果为空（需刷新后重试）/ 分类明细未获取（需刷新后重试）」，**不得**记成医学拦截、也不得凭队列里的旧分类直接放行 |
| **无最新数据不放行（关键）** | 断网或明细为空时，绝不因为「建队时是正常标本」就点审核——一律留人工，宁可漏审不可错审 |
| 补审未完成必须如实报（关键） | 批审中途点 ⏹ 停止、或补审进行到一半停止：结束 toast 必须出现「补审 N 条未完成（队列已保留，请核对）」；进度条末行显示「…, N 未完成」；**不得**因为主循环走到末尾就报「全部成功」 |
| 未完成时队列不清 | 有补审未完成时，`localStorage` 里的批审队列**保留**；刷新页面不会自动重跑这一批（不引入无限续跑），需人工核对原生列表 |
| **切组死循环真的能停（关键，回归 A5/B 组）** | 人为让切组切不过去（如当前账号无目标组权限）：应连续尝试 **3 次**后中止并置熔断，toast「⛔ 批审已中止：已连续尝试切换工作组 4 次仍未成功…」，**不得**无限在「原生 LIS ↔ 工作台」之间来回切 |
| 正常跨组批审不被误伤 | 有权限的 A→B→C 多组批审照常逐组审完，不被守卫拦下（离线 G3 场景 B 守住这条） |
| **用户手动选行不被抢（关键，8.16.31）** | 批审结束后（进度条消失），在原生页手动点选一条**别的**标本查看：脚本不得自动把选中行跳走；打开详情/检视器仍正常 |
| 确认窗只确认「已知文案」 | 批审轻异常标本时，原生「结果超出参考范围，确定要审核该报告吗？」照常自动确认，审核不断链 |
| 危险/未知确认窗不代按（关键） | 出现含「危急」「结果不完整」或未知文案的确认窗时**不自动点确定**，该条按超时走留人工；不得出现「脚本替人确认了危急提示」 |
| 目标不匹配不代按 | 用户手动在原生页操作时弹出的确认窗，脚本**不得**代按确定（需要脚本正在审该标本且弹窗标本 == 审核目标） |
| 安全阈值零改动 | 危急 / 堵孔 0 值 / 传染病阳性 / 心肌危急线 / 轻微放行带规则**一律未变**——本版只改「缺数据时的失败方向」与「确认窗准入」，不碰任何医学口径 |


## 21.13 排在列表末尾的标本必须能选中（8.16.32）

> 现场：三个标本（**生化组正常 CRP** + 免疫组异常铁蛋白/AFP），多数情况三条都能审掉，
> 偶发只剩 CRP 未审；切回原生 LIS 发现该行**根本没被选中**。当天生化组 100+ 条、CRP 排最后。
> 根因：原生工作列表是 easyui **虚拟滚动视图**（`jsLisReportResultInitM.js:1774 view:scrollview`，
> 上面的 `pagination: true` 是注释），`datagrid-scrollview.js` 的 `populate()` 会把
> `state.data.rows` 覆盖成「窗口」——而 `getRows()/getData()` 返回的正是这个被覆盖的数组。
> 脚本此前只在窗口里按 ReportDR 找行，目标落在窗口外时**恒找不到** → 选行失败 → 留人工。
>
> 离线回归：`node native_worklist_window_test.mjs`（45 断言；其中 A 组**执行真实
> datagrid-scrollview.js 的 getRows/populate**，不重写副本）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node native_worklist_window_test.mjs` → 必须 **11 项失败**。

| 步骤 | 期望 |
|------|------|
| **末尾标本必须被选中（关键，本次核心）** | 找一天某机台（如生化）列表 **>100 条**、且目标标本排在**列表最后几行**：批审/F4 后该标本必须被审掉；若未审掉，切回原生看它**应当是被选中的**，不得出现「列表里根本没选中任何行」 |
| 三个标本混排（现场用例） | 生化组正常 CRP + 免疫组异常铁蛋白/AFP 三条一起批审，重复 5~10 次：**每次三条都必须审掉**，不得偶发剩 CRP |
| 滚动可见性 | 选行过程中原生工作列表会**自动滚动**到目标行（这是原生自己的做法，`InitM:1937`）；属预期行为，不是卡顿 |
| 选行不再谎报成功 | 若某条标本选不上，日志必须出现「selectNativeRow: 选行未生效，目标未成为原生选中行…」；该条按留人工处理，**不得**出现「提示成功但列表里还在」 |
| 不重发全量查询 | 目标已在完整列表里时，等待过程**不得**反复调用 ShowWorkList 全量刷新（否则会不断把滚动位置和列表顺序冲掉，越刷越选不到） |
| 详情身份校验 | 连续审多条时，若详情尚未加载完，不得把**上一条**的详情当成当前条已就绪（原生 `text_MajorConclusion[name]` 必须匹配当前 ReportDR） |
| 单条/详情审核回归 | 待审视图 Enter 单条审核、详情面板「审核」按钮仍正常（这两条路径共用同一选行函数） |
| 跨组批审回归 | A→B→C 多组批审仍逐组审完，不因本次改动多切组或少切组 |
| 安全阈值零改动 | 危急 / 堵孔 0 值 / 传染病阳性 / 心肌危急线 / 轻微放行带规则**一律未变**——本版只改「怎么把行选中」，不碰任何医学口径 |


## 21.14 用原生「检验号快速检索」定位标本（8.16.33）

> 现场用户发现：工作列表右上角那个下拉默认是**「检验号」**，右边输入框输入条码号即可定位。
> 这比滚动找行**更可靠**——原生 `FindFast`（`jsLisReportResultM.js:2170`）在 `LabNo` 模式下走
> **精确匹配**（2220-2224），命中后 `loadData({total:1, rows:[该标本]})`，而原生
> `onLoadSuccess`（`jsLisReportResultInitM.js:1920`）见 `rows.length==1` 会**自动 selectRow(0)**
> → **目标永远落在第 0 行**，从根上绕开虚拟滚动窗口。
> 本页没有该标本时（跨组/未加载完），原生自己退回服务端查询（2181-2187 带 `ReportStatus=0`、
> 2233-2237 仅带 `Labno`），所以对跨组标本同样有效。
>
> 离线回归：`node native_worklist_window_test.mjs`（86 断言；G 组**执行真实原生 FindFast**）。
> 反向验证：`LIS_SRC=旧版 node native_worklist_window_test.mjs` → 必须 **7 项失败**。

| 步骤 | 期望 |
|------|------|
| **末尾标本改用检索定位（关键）** | 生化组 100+ 条、目标排最后：批审时原生列表会被**收敛成只剩该条**（右上角检索框里能看到脚本填入的条码号），该标本被审掉 |
| 检索类型必须钉死「检验号」 | 即使手动把下拉切成「流水号」，脚本执行时也会把它拨回**「检验号」**再检索（切错会把工作列表清成 0 行） |
| 跨组标本 | 免疫组异常标本在生化组视角下批审时，检索会带回该标本（服务端查询），不需要为它单独切组 |
| 检索失败要如实留人工 | 若条码号在本机台/本日查不到，该条按留人工处理，日志出现「检验号快速检索未定位到标本」，**不得**谎报成功 |
| 不干扰用户 | 用户正在原生页手动查看别的标本时，脚本**不得**去改检索框或抢选行（选行锁生效） |
| 检索后列表状态 | 检索会把原生工作列表收敛成 1 条；下一条标本会重新检索/刷新列表——属预期，不是卡死 |
| 安全阈值零改动 | 危急 / 堵孔 0 值 / 传染病阳性 / 心肌危急线 / 轻微放行带规则**一律未变** |


## 21.15 批审选行零等待极速流水线（8.16.34）

> 消除从第 2 条标本开始每条 240ms 的冗余盲等发呆。
> 初次在当前仅 1 行的网格中未选中时，若有条码号且支持快速检索，**立即执行 `tryNativeFindFastSelect`**（通常 10~20ms 瞬间命中）；
> 只有未检索到或无条码号时，才退回 6 轮 sleep(40) 虚拟滚动翻页缓冲。

| 步骤 | 期望 |
|------|------|
| **连续批审流畅度（核心）** | 批量审核 20~50 条标本：从第 2 条开始，标本切换极其干脆利落，不再有每条约 0.25 秒的微顿挫，进度条流畅前进 |
| **单条定位耗时** | 控制台查看 `批审: 已用检验号快速检索定位` 日志，标本定位命中耗时压至几十毫秒内 |
| **末尾标本依然保真** | 列表第 100 行以后的标本（如生化 CRP）依然能准确被检索并自动选中第 0 行，绝不漏审 |
| **异常/退回安全** | 若条码号不存在或检索落空，仍能平稳退回滚动翻页与服务端全量刷新，安全拦截与留人工机制不被破坏 |



## 22. 卡片瘦身 + 星号占位结果归「不完整」（8.15.11）

> ① 去掉待审卡片底部的「Enter=审核」提示行（每张无红线的卡片都多一整行，抬高高度）；
> ② 结果形如 `***.**` 的标本（仪器未算出/未回传，**各仪器星号个数不同**）不再进「待审」，改归「不完整」，
> 防止 Enter/F4 连未出全结果的标本一起审掉。

| 步骤 | 期望 |
|------|------|
| 待审卡片（普通异常/正常卡） | **不再出现**「Enter=审核」「Enter 审核」字样 |
| 卡片高度 | 比之前**明显更矮**（少了一行） |
| 危急值卡 | 仍显示「🚨 危急值」提示（红线提示保留，不因瘦身去掉） |
| 堵孔卡 / 历史不一致卡 | 仍显示各自的提示 |
| 审核进行中 | 该卡出现**蓝色描边**（`auditing`），且**卡片不会跳高**（用 outline 不占布局）；审完描边消失 |
| **星号占位标本（关键）** | 找一条结果里有 `***.**` 之类（星号个数随仪器不同）的标本 → 它应在**「不完整」**标签里，**不在「待审」**里 |
| 该标本不能被 Enter 误审（关键） | 在「待审」视图按 Enter，**不会**审到这条星号标本（Enter 只在 `audit` 分类生效） |
| 该标本也不会被 F4 批审 | F4 批审队列不含它（F4 只收 NORMAL / 轻微带内） |
| 危急值 + 星号 | 若一条标本既有危急值又有星号占位 → **仍留在「待审」**（危急值不可下沉，且它本就不可被 Enter/F4 批审） |
| 堵孔 + 星号 | 同上，仍留在「待审」 |
| 正常标本不受影响 | 无星号的标本分桶行为与之前**完全一致**（NORMAL→待审、UNCERTAIN但完整→待审） |
| 误伤检查 | 形如 `1.5*`（真值带脚注）、`*1.5`、`...`（纯点）、`-`、`阴性` 的结果**不应**被当成星号占位 |
| 备注 | 挪到「不完整」后，**从详情面板手动点「审核」仍可审**（这是有意的：确需人工审核时不该被锁死）。只有 Enter/F4 批量路径被挡住 |


## 23. 工作台「外送」工作组：只追踪待排/采集（8.17.0）

> 需求：外送（dr=5，组内仅一台仪器「外送标本」）纳入工作台，但**只显示「待排 / 采集」**；
> 标本一旦录入结果（不完整 / 待审 / 已审 / 取消）就必须从工作台所有视图与统计中消失——
> 外送报告由第三方出具，本科室不审核，留在审核队列里只会干扰批审与推送。
>
> 离线回归：`node external_wg_test.mjs`（48 断言：静态不变量「工作台热路径 0 处裸 `WG.`」
> + 真实 `getWSAuditBucket` 切片的状态矩阵 + 自检组不被误伤）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node external_wg_test.mjs` → 必须 **15 项失败**。

| 步骤 | 期望 |
|------|------|
| **外送标签出现（关键）** | 工作台顶部工作组标签多出 **「外送」**，排在免疫之后；悬停有说明「只追踪待排/采集…」 |
| **只显示待排/采集（关键）** | 选中「外送」→ 自动落到**「待排」**分类；「待排」「采集」两个标签有数，「待审」「不完整」**恒为 0** |
| **录入结果后消失（关键）** | 某条外送标本在 LIS 里被录入/核收结果后（刷新工作台）：它**不再出现在「外送」的任何分类里**（含「全部」），外送标签计数相应减少 |
| **「全部」视图也不显示已出结果的外送** | 选中「外送」+「全部」：只看到待排/采集两类行（状态列显示「待排样」/「采集」），**没有**「登记/初审/审核/复审/取消」的行 |
| **外送不进待审、不被误审（关键，医疗安全）** | 外送标本**永远不出现在「待审」**视图；按 **F4 批审**、按 **Enter** 都**不会审到外送标本**（外送标签下 Enter/F4 应提示无标本可审） |
| **自动审核不碰外送（关键）** | 开自动审核（含「全部工作组」范围）：审核记录里**不得**出现外送标本；外送标本也不会让推送「等它做完」而延迟 |
| **计数口径** | 「外送」标签数字 = 待排 + 采集；「全部」标签数字 = 自检组三条 + 外送待排/采集；**待审/不完整的总数不受外送影响**（与 8.17.0 之前一致） |
| **自检组不受影响（关键）** | 临检/生化/免疫三组的待审/不完整/待排/采集计数与标本列表**与之前完全一致**；批审、F4、详情审核照常 |
| **患者结果导出不受影响** | 「病人结果筛选导出」里的「外送」仍照常可查、可导出（那条链路本来就含外送，与工作台互不影响） |
| **质控导出不受影响** | 质控导出仍只有临检/生化/免疫三组仪器，**不得**多出「外送标本」 |
| **仪器选择** | 「全部仪器」下拉里出现「外送」分组（仅 1 台「外送标本」）；勾选它只过滤外送待排/采集，不勾不影响其它组 |
| **刷新/重开不卡死** | 停在外送标签刷新页面 / 关掉重开工作台：仍停在「外送 + 待排」（不会停在一个永远空白的「待审」上） |
| **手工仪器语义零改动** | H900 / 手工杂项那套「实际录入完整才升待审」的口径**未变**（外送是独立分支，不走这条） |



## 24. 自动审核「记录 / 徽章」口径收敛：人工补审后不再残留（8.17.1）

> 现象（用户报）：**自动审核没审掉的标本，后来手工审核掉了，但（a）它还在「自动审核记录」里显示，
> （b）「全部」标签里它依然挂着 `🤖 自动` 徽章**。
>
> 根因：日志是逐轮追加的，同一条标本可能先被记成「正常通过」、后续轮次又因数据未刷新被记成
> 「审核未确认成功 / 已审核，跳过」；徽章集合原来「只要在成功明细里出现过就入集合」，
> 记录查看器也只看机器人自己的日志，两者都没有「最终态」概念，也没有「这条是人工补的」的概念。
>
> 两条修复：
> ① **徽章**（`rebuildAutoAuditedTodaySet`）——以该标本在**今日日志里的最后一条结论**为准
>   （最后一笔是「留人工/跳过」就不算机器人审掉），并再减掉「人工审核留痕」里的标本。
> ② **记录查看器**——`未通过` 条目一旦被人工补审，**默认折叠不显示**（不再是待办），
>   工具栏 `✅ 已人工处理` 开关可临时展开（灰调 + `✅ 已人工审核` 徽章，保留可追溯性）。
>
> 人工审核留痕（`LIS_HumanAuditLog`）：所有人工审核入口都经过 `executeNativeAudit`，
> 在那里按「是否处于自动审核轮次（`_autoAuditRunning`）」打点；自动审核队列在轮次外续跑
> （`queue._autoMode`）的三处成功点会回撤，避免把机器人审掉的标本误记成人工。
>
> 离线回归：`node auto_audit_log_hygiene_test.mjs`（53 断言：静态不变量 + 真实切片的日志场景矩阵）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node auto_audit_log_hygiene_test.mjs` → 必须 **29 项失败**。

| 步骤 | 期望 |
|------|------|
| **人工补审后记录里消失（关键，用户报的那条）** | 自动审核留人工的标本 → 在「待审」里手工审掉 → 重开「📋 自动审核记录」：该条**不再显示**在列表里；概要条出现 `✅ 已人工处理 N（已折叠）` |
| **可临时查看（可追溯）** | 点工具栏 `✅ 已人工处理`：被折叠的条目出现，卡片**灰调**、徽章是 `✅ 已人工审核`（不再是橙色「未通过」）；再点一次折叠回去 |
| **概要计数口径** | 折叠态下「当前共 N 个标本」**不含**已人工处理的；`⏭ 未通过` 数同步减少（它们不再是待办） |
| **全被处理完时不报「暂无记录」** | 今天所有记录都被人工处理完 → 空态文案是「所选范围内的 N 条记录都已被人工审核处理完（已折叠…）」，**不是**「暂无自动审核记录」 |
| **「全部」标签徽章消失（关键，用户报的那条）** | 人工审掉的那条标本 → 切到「全部」：它**不再带 `🤖 自动` 徽章**（`审核` 状态标签仍在） |
| **机器人审掉的标本徽章保留（关键，别误伤）** | 机器人自己审掉的标本 → 「全部」里**照常有 `🤖 自动` 徽章**；自动审核记录里照常显示「✓ 正常通过 / ⚡ 轻度异常通过」，**不**被折叠 |
| **跨天不串（关键）** | 昨天的记录不参与今天的徽章判定；跨天后 `🤖 自动` 徽章全部清空（口径未变） |
| **多标签页场景** | 两个标签页同时开自动审核：A 页审掉、B 页重复记账 → 「全部」徽章以**最后一条结论**为准（不会挂着徽章说已处理完） |
| **留痕不干扰推送** | 手工补审**不产生任何手机推送**（留痕只影响徽章与记录查看器，不碰 `queueAutoAuditPush`） |
| **留痕不干扰自动审核范围** | 手工补审过的标本**不会**因此被移出自动审核候选（只影响显示口径，`rowIsAutoAuditCandidate` 未改） |
| **无红线回归（关键，医疗安全）** | 自动审核的红线拦截（负值 / 梅毒丙肝艾滋阳性 / 心肌标志物）行为**完全未变**：该留人工的仍留人工、仍进推送 |
| **自动审核队列在轮次外续跑** | 整页刷新后自动审核队列自动续审（`runAuditQueueResume`）：那批被机器人审掉的标本**仍带 `🤖 自动` 徽章**（回撤生效，没被误记成人工） |
| **异常逐条段** | 「审核未确认成功（留人工/下轮重试）」的条目现在带 ReportDR → 记录里「⤴ 工作台」跳转按钮可用（原来点不动） |
| **同一条标本不再重复计数** | 同一条标本同一轮里既被判成功又被判留人工 → 日志里只留一条（累积器按 reportDR 去重，不再把 labname 当另一条标本） |


## 25. 修正 8.17.1「人工补审后没折叠」的两个根因（8.17.2）

> 现象（用户报）：「开了自动审核情况下，刚才一个无法通过自动审核的标本我手动审掉了，
> 还是在自动审核记录的下拉窗口里面，而且也没有折叠，和自动审核的标本列在一起。」
>
> 8.17.1 的折叠逻辑本身没错，是**两条判据都失效了**：
>
> **根因 1（打点漏了整条链路）**：人工审核留痕打在 `executeNativeAudit` 里，但**批审主循环
> 是直接调 `clickNativeAuditButton`**（`continueAuditQueue` 内部），压根不过 `executeNativeAudit`
> ——也就是说用 **F4 / 批审按钮**审掉的标本从来没打过留痕。
> → 8.17.2 在 `continueAuditQueue` 的三处批审成功点按 `!queue._autoMode` 补打。
>
> **根因 2（判据反了）**：折叠判定里有一条「机器人今天审过它就不折叠」。
> 可本判定只作用于 `t === 'skip'` 的条目——机器人**审掉**的标本在记录里是**另一条** `audited`
> 条目（`t='normal'/'abnormal'`），那条根本不受折叠影响。加了这条，恰好把
> 「机器人先失败 → 后来人工补审」这条 skip 条目钉死在列表里。
> → 8.17.2 删掉这条，并把「已审核」放宽到 **3(审核) / 4(复审)**。
>
> **顺带修的判定**：`_autoAuditRunning` 不能用来判断「这次审核是不是机器人发起的」——
> 一轮自动审核可能持续几十秒到几分钟（整批 `await executeBatchAudit` 期间它一直是 `true`），
> 用户在轮次进行中插入的手动审核会被误判成机器人。
> → 改为 `isRobotAuditCtx()`：机器人**显式包裹**自己发起的那次审核（`robotAuditBegin/End`）。
>
> 离线回归：`node auto_audit_log_hygiene_test.mjs`（60 断言）+ `audit_queue_context_test.mjs`（89 断言）。
> 反向验证（必做）：`LIS_SRC=/tmp/v8171.user.js`（`git show HEAD:...`）→ 应 **17 项失败**；
> `LIS_SRC=/tmp/v8170.user.js`（`git show HEAD~1:...`）→ 应 **32 项失败**。

| 步骤 | 期望 |
|------|------|
| **F4 批审审掉的留人工标本（关键，8.17.1 就漏在这里）** | 自动审核留人工的标本 → 用 **F4 批审**审掉 → 重开「📋 自动审核记录」：该条**不再显示**；概要条出现 `✅ 已人工处理 N（已折叠）` |
| **详情面板 / 回车审掉的** | 同上（这条链路 8.17.1 本来就通，确认没被 8.17.2 改坏） |
| **自动审核进行中插入手动审核（关键）** | 开着自动审核、趁机器人正在批审时，手动审掉一条留人工标本 → 它**照样折叠**（旧版会被误判成机器人而不折叠） |
| **机器人自己审掉的标本照旧** | 机器人审掉的标本在记录里仍是「✓ 正常通过 / ⚡ 轻度异常通过」，**不折叠**；「全部」里照常有 🤖 徽章 |
| **机器人先失败、后来人工补审（关键，根因 2）** | 一条标本机器人先报成功、后续轮次又记「留人工」→ 人工审掉后：🤖 徽章消失、记录里那条「未通过」**折叠**（旧版被「机器人今天审过」钉住） |
| **复审(4) 标本** | 人工审成复审状态的留人工标本，也判定为已处理（8.17.2 放宽） |
| **还没处理的留人工标本** | **照常显示**「未通过」——它们才是真正要看的待办，别被误折叠 |
| **自动审核队列不算人工** | 开自动审核让它自己审掉一批 → 那批标本**仍带 🤖 徽章**、记录里仍是「通过」（没被误记成人工） |
| **无红线回归（关键，医疗安全）** | 红线拦截（负值 / 梅毒丙肝艾滋阳性 / 心肌标志物）与推送行为**完全未变** |
| **自诊断：为什么这条没折叠（8.17.3）** | 鼠标悬停在那条「未通过」的原因文字上 → 提示会写明原因：`未折叠原因：该标本当前状态 = 2（初审），还没审核 → 仍算待办，所以留着`（或「工作台当前数据里找不到该标本…」/「这条日志没记录 ReportDR…」） |
| **自诊断与折叠判定必须一致** | 凡是**没有**悬停提示（title 为空）的「未通过」条目，一定已经被折叠掉了、不该出现在列表里；反过来说列表里出现的每条「未通过」都能悬停看到原因 |


## 26. 待审界面直达电子病历（📄 病历 / 快捷键 M）（8.17.4 → 8.17.5 挪位）

> 需求（8.17.4 用户原话）：「在待审界面的标本 能不能也直达病历系统啊 我好像没看到入口」
> 需求（8.17.5 用户原话）：「打开病历的按钮可不可以加在详情页 跳过 审核此标本两个按钮的边上
> 不用加在样本标签里面」
>
> 落地：入口加在**待审视图右栏「详情页」**（实时检视器）的底部动作排 —— 就是
> 「↷ 跳过 (Space)」「✓ 审核此标本 (Enter)」那一排的最左边；8.17.4 曾放在左栏标本卡片上，
> 8.17.5 按用户要求挪走，卡片恢复原样（类名/CSS/助手函数一并删除，不留死代码）。
>
> 三个入口都调**同一个** `openEnhancedEMR`，取数口径（Labno / PatName / RegNo / EpisodeNo）
> 完全一致，不会出现「一处能开、另一处开不了」：
>   ① 右栏检视器底部 `📄 病历 (M)` 按钮（点击=Chrome 弹窗，Shift+点击=原生 32 位 IE）
>   ② 待审视图 `M` 键（当前聚焦标本；`Shift+M` = 原生 IE）
>   ③ 详情面板 `📄 病历` 按钮 + `M` 键（与既有 `H`=历史 并列）
>
> 离线回归：`node workbench_emr_entry_test.mjs`（42 断言）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node workbench_emr_entry_test.mjs` → 必须 **13 项失败**。
>
> ⚠️ 前置条件：`Shift+点击` / `Shift+M` 直接唤起原生 IE 需要该机器跑过一次
> `http://192.168.31.111:9111/lis-tools/配置工作台-原生IE直达.bat`（注册 `lis-ie://` 协议 +
> 装 DHCC 插件），详见 `DHCC病历插件诊断结论.md`。没配过的机器上普通点击仍可用（Chrome 弹窗）。

| 步骤 | 期望 |
|------|------|
| **右栏底部有入口（关键，用户要的位置）** | 待审视图右栏「详情页」底部，从左到右依次是 **`📄 病历 (M)`**、`↷ 跳过 (Space)`、`✓ 审核此标本 (Enter)` |
| **左栏卡片上没有它（关键，用户明确不要）** | 左栏标本卡片（正常卡/异常卡）**没有**病历按钮；卡片第二行仍是「组合项目 …… 核收时间」，与 8.17.3 完全一致 |
| **点按钮不误触别的** | 点 `📄 病历` → 只打开病历弹窗；**不会**跳过标本、**不会**审核、**不会**改变左右栏选中项 |
| **打开的是当前这条（关键）** | 用 ↑↓ 切到另一条标本，再点 `📄 病历` → 打开的是**当前蓝框那条**的病历（不是上一条） |
| **Shift+点击 / Shift+M** | 直接唤起原生 32 位 IE 打开病历（前提：该机器已跑过「配置工作台-原生IE直达.bat」）；普通点击/普通 M 走 Chrome 弹窗 |
| **快捷键 M** | 待审列表里按 `M` → 打开当前聚焦（蓝框）那张卡片对应患者的病历；提示条与右栏提示行都写了 `M 病历` |
| **详情面板 M** | 打开详情面板后按 `M` → 打开当前详情标本的病历（与既有 `H`=历史 并列）；页脚提示显示「↑↓ 切换 \| Enter 审核 \| M 病历 \| H 历史 \| Esc 关闭」 |
| **病历弹窗功能未受影响** | 弹窗内 `⚡ 原生IE打开` / `📋 复制链接` 照常可用；病人信息、就诊类型、日期、8 个选项卡（医嘱/病历/检查/检验/诊断/过敏/麻醉/会诊）与之前一致 |
| **没有检验号的标本** | 点按钮 → 提示「该标本没有检验号，无法查看病历」，**不报错、不白屏** |
| **既有键位与按钮未变（关键）** | `Enter` 审核、`Space` 跳过、`↑↓`/`j`/`k` 移动、`Shift+Enter` 开详情、`F4` 批审、`1-5` 切分类、`H` 历史、`Esc` 关闭——全部与之前一致（M 是新增键，不占别人的位置）；「跳过」「审核此标本」两个按钮的位置与行为也没变 |
| **输入框里按 M 不劫持** | 光标在工作台搜索框 / 详情面板输入框里时按 `m` → 正常输入字母，不会弹病历 |
| **待排/采集/不完整/全部视图** | 本次**未**在这些视图加入口（它们是表格）。要直达病历可先点行开详情面板，面板里有 `📄 病历` |


## 27. 延迟压缩：CA 认证 + 批审（8.17.6 / 8.17.7）

> 背景（用户提问）：「批审报告的速度提升了很多…是不是还有提升的空间？还有就是每次 ca 认证的过程
> 还是有些卡顿 有时候要耽误好几秒钟，看看整个 ca 认证过程还能不能再压榨出一些速度提升」
>
> 排查结论：8.16.33~8.16.36 已经把「选行 / 预选 / 秒审」做到接近原生极限，剩下的浪费集中在两处：
>
> **① CA 认证的主因是「密钥派生风暴」与「死等待」**：
> - 8.17.6：`getCryptoKey` 改用 Map 缓存（keyId → CryptoKey），消除多账号遍历与 30s 自动刷新时的 10 万轮 PBKDF2 风暴；capping 表单已可见时跳过 280ms 盲等；成功轮询首轮先验后等。
> - 8.17.7：**彻底清空 `submitOnce` 死等待**：消灭 post-login 500ms 硬盲等（检测到 Ukey 若原生已关窗 0ms 直接退出，未关则微轮询最多 80ms 自然关窗，省 420~500ms）；输入密码到点击登录从 200ms 压到 50ms（省 150ms）；表单切换未就绪时改为 7×40ms 微轮询早退；工作台启动在后台空闲期（`requestIdleCallback`）提前预热派生密钥，批审时 0ms 命中。
>
> **② 批审四处「先盲等再校验」**：原生其实已经审成功时，原实现还要白等 120~250ms 才看第一眼。
> 改成「先校验再等」。**总等待窗口一律保持不缩水**——窗口缩了会把已审成功的标本误判成失败 →
> 假留人工，那是医疗安全问题。
>
> 离线回归：`node audit_latency_test.mjs`（44 断言，含「N 个账号只派生 N 次、重复调用不再派生」的仿真与 8.17.7 延迟压缩断言）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node audit_latency_test.mjs` → 必须多项失败。

| 步骤 | 期望 |
|------|------|
| **CA 认证整体提速（关键）** | 开自动审核/批审触发 CA：认证耗时比 8.17.6 进一步缩短 500ms+（几乎瞬间闪过并完成）；观感上「停顿感」基本消除 |
| **CA 认证仍然成功（关键，安全）** | 认证后照常能审报告；**不出现**「CA 认证未完成，请在弹窗内手动 capping 登录」的误报 |
| **多账号环境** | 设置里存了 2 个以上 CA 账号时：认证耗时不再随账号数线性增长（旧版每账号一次 10 万轮 PBKDF2） |
| **工作台长时间开着不再周期性卡顿** | 挂着工作台（30s 自动刷新）不动鼠标：不再每 30 秒出现一次短促卡顿（旧版是 `updateCaUserBadge` 触发的 PBKDF2） |
| **换工作组要重新 CA 时** | 切到另一个工作组、该组首次审核触发 CA：同样走优化后的快路径，不重复做已缓存的密钥派生 |
| **批审不出现假留人工（关键，安全）** | 批审 20~50 条：**不出现**「提示失败/留人工，刷新后其实已审核」的标本（窗口没缩水，判定逻辑一行没改） |
| **批审流畅度不回退** | 连续批审的手感与 8.17.5 一致或更好；进度条不卡顿 |
| **慢确认场景** | 偶发「原生回写慢」的标本：确认耗时比之前更短（先校验再等，第一眼就验出），但最终结果判定与之前**完全一致** |
| **审核超时窗口未变（关键）** | 单条审核超时仍是 4500/7000/12000ms；CA 登录总截止仍是 45s/90s；补审单条超时仍是 4000/8000ms |
| **密码功能未受影响** | 「设置 → CA 账号」列表能正常显示已存账号与备注；新增/删除/切换账号正常；密码仍按 AES-GCM 加密存储（本次只改缓存，未降强度） |


## 28. 漏结果审核防线：完整度闸门 fail-closed + 审核后自检（8.17.9）

> ⚠️ **本节已过时（8.18.0 起）**：`specimenCompleteness`（fail-closed 闸门）、审核后自检
> `verifyAuditedCompleteness`、审核留痕 `LIS_AuditTrace`（`lisAuditTrace()`）、应有项数观察模式
> `LIS_ExpectedItems`（`lisExpectedItems()`）**已全部从脚本删除**——本节下面凡涉及这些函数的
> 手测项都**不再适用**。**现行手测见 §29。** 保留本节仅为历史留档；那次的教训仍然成立：
> 闸门必须 fail-closed；「LIS 谎报 IsComplete=1」只能靠明细级检查抓。
>
> 现场问题（用户原话）：「一个正常的性激素标本 当时应该是 f4 批审审核掉了 但是我今天看 lis 上面的
> 结果没全 缺了三个项目 竟然审核掉了 …… 漏结果审核标本是一个非常重大的问题」
>
> 逐条读代码得到的**闸门表**（改前）：
>
> | 入口 | 完整度闸门 | 问题 |
> |---|---|---|
> | F4 / 一键批审（主循环） | `liveRow && !isSpecimenActuallyComplete(liveRow)` | **liveRow 为空时整条判定被静默跳过**（fail-open） |
> | F4 补审轮（`auditOneQueueItemOnce`） | **无** | 主循环拦下的标本会在补审轮被原样放行 |
> | 详情面板「审核」/ 面板内回车 | **无** | 只有 collected/pending 早退与分类校验 |
> | 待审列表回车（`auditAbnormalSpecimen`） | 有 | 用的是内存行，可能过期 |
>
> 另有一条系统性弱点：**批审期间工作台 30s 刷新被停掉**，队列里的行可能是几分钟前的快照，
> 而全程没有任何一处用最新数据复核。
>
> 本次改动：
> ① 完整度判定集中成 `specimenCompleteness(row, cached)` → **返回 `{ok, missing, reason}`**（可诊断，
>   拦下时能说清「缺 3 项」还是「无结果（应做 6 项）」还是「完整度未知」）；
> ② 主循环复核改 **fail-closed**（拿不到行也绝不放行 → 技术性缺数据 requeue → 补审 → 留人工）；
> ③ 新增**审核前最终闸门**（拿到最新分类之后、点审核之前，按最新可用行再判一次）；
> ④ 补审轮补上同一道闸门；⑤ 详情面板补「提示 + 留痕」（**不硬拦**，保留有意的人工通道）；
> ⑥ 新增**审核后自检**：批量收尾用最新工作列表复核刚审掉的标本，发现不完整立刻红色告警 + 手机推送。
>
> 离线回归：`node audit_completeness_guard_test.mjs`（51 断言）。
> 反向验证（必做）：`git show HEAD:iMedicalLIS-enhancer.user.js > /tmp/old.user.js` 后
> `LIS_SRC=/tmp/old.user.js node audit_completeness_guard_test.mjs` → 必须 **24 项失败**。

| 步骤 | 期望 |
|------|------|
| **缺项标本不会被 F4 审掉（关键）** | 找一条结果不完整的标本（LIS 里显示缺 N 项）：它应落在「不完整」分类，**不出现**在待审；即使出现在待审，F4 也会拦下并提示「审核前复核：结果不完整（缺 N 项）」 |
| **拦下时的提示能看出缺几项（关键）** | 被拦下的标本在进度/提示里带**具体缺项数**（不是笼统的「结果不完整」） |
| **补审轮不会绕过闸门（关键）** | 批审中若有标本被完整度拦下，它**不会**在补审轮被原样审掉；留人工记录里的原因带「完整度复核：…」 |
| **详情面板给出提示（关键）** | 在不完整标本的详情面板点「✅ 审核」→ 先弹提示「⚠️ 该标本结果不完整（缺 N 项），仍按人工判断审核」，然后**照常审核**（有意保留的人工通道，未被锁死） |
| **回车路径不受影响** | 待审列表按回车审不完整标本 → 照旧提示「跳过: xx 结果不完整」并跳到下一条（原行为不变） |
| **审核后自检（关键，新增）** | 批审跑完（或中途停止）后：若本批审掉的标本里有结果不完整的 → 立刻弹**红色**告警「🚨 审核后自检：N 条刚审掉的标本结果不完整（…）」，并（若开了推送）收到 `LIS危急告警` 档手机推送 |
| **自检不误报** | 正常批审（全部完整）跑完 → **不弹**告警；`dbg` 里能看到「审核后自检通过: N 条」 |
| **自检不误报（边界）** | 标本已出查询范围 / 工作列表读失败 → 不下结论、不告警（宁可漏报也不误报） |
| **批审流畅度不回退** | 自检在批量收尾才跑（每台仪器一次工作列表读），批审过程手感与之前一致 |
| **手工仪器口径不变（关键）** | H900 / 手工杂项：项目全部录全 → 照常进待审可审；未录全 → 拦下并提示「手工录入项目未录全」 |
| **外送组不受影响** | 外送标本仍只追踪待排/采集，不进任何审核路径 |
| **红线拦截不变（关键，安全）** | 危急值 / 负值 / 梅毒丙肝艾滋阳性 / 心肌标志物 的拦截行为**完全未变** |
| **审核留痕（8.17.10）** | 批审若干条后，在控制台（F12）执行 `lisAuditTrace()` → 列出每条审核记录：日期/时间、检验号、姓名、仪器、**走的哪条路径**（人工批审(F4) / 补审轮 / 详情面板·回车 / 机器人批审）、**审核那一刻的 IsComplete 与 NoResRows** |
| **按检验号查留痕** | `lisAuditTrace('26091800246')` → 只列该检验号的记录（现场定位用） |
| **留痕不影响审核** | 有留痕写入时批审手感与之前一致（环形 800 条，写盘是同步的 localStorage，量很小） |
| **应有项数学习（8.17.11，观察模式）** | 跑一天工作台后，控制台执行 `lisExpectedItems()` → 列出 `{组合项目, 应有项数, 观测次数}`（数据来自 LIS 自己 `IsComplete='0'` 行的 `NoResRows`，即该组合项目的项目总数） |
| **观察模式不拦人（关键）** | 项数对不上的标本**照常可审**（8.17.11 只记录、只提示，**不拦截**）——不要出现「明明完整却被拦下」的情况 |
| **观察模式的数据在留痕里** | `lisAuditTrace()` 每条记录带 `exp`（应有）/`act`（实测）/`short`（差几项）/`seen`（观测次数）。下次再出「事后才发现缺项」，看这条就能定性：审核时 `short>0` → 应有项数核对本来就能抓到；`short=0` 且 `ic='1'` → 是 LIS 侧把完整度判错了 |

---

## 29. 不完整标本门禁 + 补审轮三态修复 + 漏结果审核实时监测（8.18.0 / 8.18.1）

> 背景：8.18.0（antigravity 实现）把不完整标本防线改成「门禁隔离 + 自动化绝缘 + 手动通道保留」；
> 8.18.1 修掉它引入的一个真 bug（补审轮把 `'incomplete'` 当成功），并新增**漏结果审核实时监测**。

### 29.1 门禁隔离（8.18.0）

| 步骤 | 期望 |
|------|------|
| **缺项标本不进待审（关键）** | 结果空/缺项的标本 → 落在「📋不完整」标签，**不出现**在待审；切到「不完整」能看到它 |
| **F4 收不到它（关键）** | 待审视图 F4 批审 → 队列里**不含**这些标本（它们根本不在待审） |
| **手工补全后自动解锁** | 把缺的项目补录 → 刷新工作台 → 标本从「不完整」移到待审，可正常 F4 |
| **详情面板人工通道保留（关键）** | 在不完整标本详情面板点「✅ 审核」→ 提示「⚠️ 结果可能未录全，请核对弹窗提示」，**仍可审核**（有意保留的人工通道） |
| **自动化遇弹窗主动取消（关键）** | 万一批审时弹出原生「…必填项目未存数据，是否确定审核？」→ 脚本**主动点【取消】**，标本记为「留人工」，**绝不点【确定】** |
| **人工操作不代按（关键）** | 检验人员自己在原生页审核时若弹出该弹窗 → 脚本**不代点**，留给人工自己决定确定/取消 |
| **误锁代价（预期行为，不是 bug）** | 「部分项目未出」的标本（如甲功五项缺 1 项）**不能 F4**，只能从详情面板逐条手动审。这是安全换可用性的有意取舍 |

### 29.2 补审轮三态返回值（8.18.1 修复）

| 步骤 | 期望 |
|------|------|
| **补审不把「被拦下」记成成功（关键）** | 若某标本在补审轮被原生弹窗拦下 → 它应进 `failed`/`skipped` 并记「留人工」，**绝不能**出现在 `done` 里、也**不能**被 `markSpecimenAuditedInMem` 从工作台隐藏 |
| **工作台不消失** | 被拦下的标本**仍留在**工作台（分类不变），刷新后还能看到 |
| **统计口径正确** | 批审结束的汇总里，被拦下的计入「跳过/失败」，不计入「成功」 |

### 29.3 漏结果审核实时监测（8.18.1 新增）

> 原理：LIS 自己的 `IsComplete` 是**审核那一刻**算好写库的。所以「工作列表里 IsComplete='0'（结果空）
> 或 '2'（不完整）」**且**「Status='3' 已审核」= 审核时结果就不完整却被放行 → **漏结果审核的铁证**。

| 步骤 | 期望 |
|------|------|
| **控制台入口可用** | F12 执行 `lisScanAuditLeak()` → 返回命中数组（无命中则 `[]`） |
| **命中时告警（关键）** | 若存在「已审核 + 结果空/不完整」的标本 → 弹**红色** toast「🚨 漏结果审核：N 条**已审核**标本结果为空/不完整…」+ 手机收 `LIS危急告警` |
| **批审后自动扫** | 跑完一次批审 → 约 5 秒后自动扫一次（`dbg` 里能看到） |
| **工作台刷新后自动扫** | 刷新/切换工作台 → 也会扫（**3 分钟节流**，不是每次都发请求） |
| **不重复轰炸** | 同一标本只告警一次；`lisAuditLeakSeen()` 看已告警集合；`lisClearAuditLeakSeen()` 清空后重扫会再报 |
| **⚠️ 服务端过滤参数待现场确认（关键）** | 本功能用原生同款参数 `P5=^0` / `^2` 做完整度过滤（原生 `SearchByResultType` 就是这么调的）。**首次现场务必验证**：`lisScanAuditLeak()` 若返回明显过多/过少，说明服务端未按 P5 过滤——**不影响正确性**（本地还会按 `IsComplete` 复核一遍），只影响流量 |
| **拿不到数据不误报（关键）** | 查询失败（网络/未登录）→ **不弹任何告警**、也不误报「没问题」（`dbg` 有「查询失败」） |
| **绝不干预审核（关键，安全）** | 开启监测前后，批审/自动审核的**放行行为完全不变**——它只发告警，不接任何闸门 |
| **红线拦截不变（关键，安全）** | 危急值 / 负值 / 梅毒丙肝艾滋阳性 / 心肌标志物 的拦截行为**完全未变** |

### 29.4 弹窗 fail-closed 兜底（8.18.2）

> 背景：`handleNativeMessageConfirm` 只在「脚本审核中 + 目标匹配」时才代点【取消】并返回 `'incomplete'`；
> **条件不满足时它只返回 false、把弹窗原样留着**。而等待循环默认 `ignoreMessages:true`（不据弹窗判结果），
> 8.18.0 又把那里的 incomplete 检查删成「只关弹窗」→ 弹窗被关掉后，「目标行已稳定移出列表」就可能把
> **被原生拒绝的审核**误判成成功 → 漏审。8.18.2 补回 fail-closed 兜底。

| 步骤 | 期望 |
|------|------|
| **被原生拒绝的审核绝不记成功（关键）** | 构造一条「明细缺项但 `IsComplete='1'`」的标本走批审 → 它应记为**留人工/跳过**，`queue.done` 里**不能有它** |
| **弹窗出现即终止等待** | 日志（`dbg`）里能看到「检测到原生缺项弹窗（未被 handleNativeMessageConfirm 拦下）→ 返回 incomplete」 |
| **不误报成功** | 该标本**不会**被 `markSpecimenAuditedInMem` 隐藏，工作台刷新后仍在 |
| **不引入假失败** | 正常标本（无缺项弹窗）批审手感与之前一致，成功条数不变 |
| **红线拦截不变（关键，安全）** | 危急值 / 负值 / 梅毒丙肝艾滋阳性 / 心肌标志物 的拦截行为**完全未变** |

### 29.5 回归

```bash
node audit_completeness_guard_test.mjs    # 97 断言
```

反向验证：`LIS_SRC=/tmp/old8180.user.js node audit_completeness_guard_test.mjs` → 必须 **≥11 项失败**
（8.18.0 既没有监测模块、没有 fail-closed 兜底，补审轮也还是 `!!result`）。
