# Windows 使用：外送少收分析

把 Mac 上这套 **外送对账 / 少收分析** 挪到 Windows 的步骤。

## 原理（和 Mac 相同）

| 项目 | 说明 |
|------|------|
| **基准** | 外送机构汇总表（`.xlsx`） |
| **对照** | 医院 LIS「结果」导出（`.csv`，**日期建议比机构单更宽**） |
| **只查** | 机构有、医院没有 → **可能少收** |
| **不查** | 医院有、机构没有（不算我院损失） |
| **匹配** | 姓名 + 项目名（别名/模糊），日期不强制同一天 |

结果 Excel 主要工作表：

- **一眼看懂** — 少收总额  
- **少收明细** — 机构有、医院未匹配的每一行  
- **按病人汇总 / 按项目汇总**  
- **项目名称对照** — 机构名 ↔ 本院名  
- **已匹配清单** — 抽查用  

---

## 1. 拷贝哪些文件

从 U 盘 / 网盘 / git 拿到本工具箱后，Windows 上至少要有：

```text
某目录\LIS脚本\          （名字随意）
├── 外送对账.py          ← 主程序
├── 外送对账.bat         ← 双击运行（Windows）
└── （可选）Windows-外送对账.md
```

**不依赖** `serve.py`、Tampermonkey、质控 vendor；这是独立的 Python 小工具。

若整包拷贝了审核脚本也没关系，对账单独用上面两个文件即可。

---

## 2. 安装 Python 3

1. 打开 https://www.python.org/downloads/  
2. 安装时**勾选** `Add python.exe to PATH`  
3. 打开「命令提示符」验证：

```bat
python --version
```

应显示 `Python 3.x`。

---

## 3. 安装依赖（首次）

在「命令提示符」进入脚本目录后执行：

```bat
cd /d D:\路径\LIS脚本
python -m pip install pandas openpyxl
```

也可以直接双击 `外送对账.bat`：若缺依赖会尝试自动安装。

---

## 4. 日常怎么用

### 准备两份表

1. **机构汇总**：第三方给的月度汇总 `.xlsx`（列含：送检日期、患者、条码号、单项名称、标准物价、结算金额等）  
2. **LIS 导出**：浏览器打开 LIS → 增强助手 **「结果」** → 勾 **外送** → 日期比机构账单**前后多留几天～半个月** → **导出 CSV**  

把两个文件放到「下载」或任意文件夹。

### 运行

1. 双击 **`外送对账.bat`**  
2. 按提示在文件框里 **Ctrl 多选**（同时选中）：  
   - 机构 `.xlsx`  
   - LIS `.csv`  
3. 选择保存位置（默认名类似 `外送少收分析_20260710_153000.xlsx`）  
4. 看弹窗摘要，并用 Excel 打开结果表  

### 命令行（可选）

```bat
cd /d D:\路径\LIS脚本
python 外送对账.py --机构 "%USERPROFILE%\Downloads\外送机构汇总.xlsx" --lis "%USERPROFILE%\Downloads\lis 导出.csv" -o "%USERPROFILE%\Downloads\外送少收分析.xlsx"
```

---

## 5. 和 Mac 的差异

| 项目 | Mac | Windows |
|------|-----|---------|
| 启动 | 双击 `外送对账.command` | 双击 `外送对账.bat` |
| 多选文件 | **⌘** 点选 | **Ctrl** 点选 |
| Python 命令 | `python3` | 多为 `python` 或 `py -3` |
| 打开结果 | 脚本可 `open` | bat 里 `start` 打开 |

对账逻辑、项目别名、少收口径 **完全同一套** `外送对账.py`。

---

## 6. 常见问题

### 双击 bat 一闪而过

- 在资源管理器地址栏输入 `cmd` 回车，再执行：  
  `外送对账.bat`  
- 看报错是否为「未找到 Python」或 pip 失败  

### 弹窗选文件后没反应 / 报错缺模块

```bat
python -m pip install pandas openpyxl
```

### 中文乱码

bat 已设 `chcp 65001`。若仍乱码，可忽略控制台显示，以生成的 Excel 为准（UTF-8）。

### 少收为 0 但业务觉得有漏

1. 看 **项目名称对照** 是否都有本院对应名  
2. LIS 导出日期是否**包住**机构账单前后跨日（培养/药敏常跨天）  
3. 把新的机构项目名反馈维护人员，补进 `外送对账.py` 里的 `ITEM_ALIASES`  

### 和审核脚本、质控的关系

- **审核 / 质控**：需要 `serve.py` + Tampermonkey，见 `Windows安装-含质控.md`  
- **外送对账**：只要 Python + 本 bat，**不必**开 serve、不必装油猴  

---

## 7. 建议拷贝清单（给同事）

```text
外送对账.py
外送对账.bat
Windows-外送对账.md
```

首次在 Windows 装好 Python 和 `pandas` `openpyxl` 后，以后只拷更新后的 `外送对账.py`（和 bat）即可。
